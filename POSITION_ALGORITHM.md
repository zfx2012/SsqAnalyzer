# 点位推荐算法状态

## 102期后的预注册候选复核（2026-09-07）

- 本地数据截止 `2026102`，2026年度严格开奖前滚动样本为102期。本轮仅重评既有 `annual-short-dynamic-hierarchical-previous-year-shared-transfer-blend05-global`，不改模型参数、正式版、当前 `v5.3` 影子版或密封预测。
- 候选全年覆盖/点亮为 `3.451 / 2.961`，当前影子为 `3.412 / 2.961`。六列命中率差为 `+0.010 / -0.029 / +0.029 / -0.020 / 0 / +0.010`；第2、4列下降，未通过逐列非劣门槛。双方最近10期均为 `3.100 / 2.900`，候选最后一段覆盖/点亮差为 `-0.048 / -0.095`。
- 完全重复率均为 `0%`，但平均保留上期点位由影子的 `1.812` 升至 `1.832/6`，候选未通过保留点位门槛，不具备进入前向验证的资格。候选族覆盖/点亮 Reality Check p值为 `0.8411 / 0.9275`，SPA p值为 `0.1849 / 0.6397`，没有显著优势证据。
- 锁定信息截止点仍为 `2026096`；`2026097–2026102` 共6个预注册样本外目标期，候选平均覆盖/点亮为 `3.000 / 3.000`，与影子的覆盖、点亮及六列命中率差均为0。继续保留原预注册设定观察，不因本轮回测重新调参或晋升；102个年度回测样本不能替代开奖前密封配对样本。
- 临时实时研究入口已移除；主项目与验证项目编译均为0警告、0错误，完整验证 `21/21` 通过。`2026103` 正式版和影子版待开奖密封预测保持不变。

## 选择器重复根因审计（2026-09-07）

- `SelectGlobalRangeEvents` 在主分数完全相同时只比较可选的次级分数；若没有次级分数，深度优先遍历会优先较小点位。因此重复并不一定来自近期特征失效，也可能来自同分解的确定性顺序偏置。
- 该发现暂不构成生产修复：把上一期预测重合数直接加入主分数会牺牲六列命中目标，且从现有回测看，宽泛的多样性约束会降低点亮。后续只允许在主分数和已有研究次级分数都精确同分时，增加“上一期预测重合数最少”的影子候选，并进行严格开奖前滚动检验。
- 当前正式版、影子版和密封账本均未改动。
- 严格滚动测试了研究候选 `annual-short-dynamic-hierarchical-ball-global-anti-repeat-tie-break`：只在动态分数和原次级分数同分时最小化与上一期预测的重合。102期结果为覆盖/点亮 `3.422 / 2.882`，当前影子为 `3.412 / 2.961`；六列为 `0.490,0.461,0.520,0.392,0.520,0.500`，第1、4列下降；完全重复率升至 `11.9%`，平均保留点位升至 `4.228/6`。候选同时损害点亮、重复和保留门槛，已完整撤回，不继续搜索同分反重复 tie-break。

## 097期后的完整上一年度共享迁移否决

- 测试 `annual-short-dynamic-hierarchical-previous-year-full-shared-transfer-blend05-global`：使用完整2025年度且均匀时间权重训练9项共享特征系数，不迁移任何号码效应；当前年度仍只用目标期前近30期完整训练，再与 `v5.3` 逐球固定等权平均并做原六范围全局选择。该候选不调整年份、权重或窗口。
- 97期严格滚动结果为覆盖/点亮 `3.443 / 2.948`，当前影子为 `3.433 / 2.959`；相对差为 `+0.010 / -0.010`。六列差为 `+0.010 / -0.021 / +0.010 / -0.010 / -0.010 / +0.010`，第2、4、5列退化；最近10期为 `3.400 / 3.000`，低于影子的 `3.600 / 3.200`，最后一段两项均退化 `-0.100`。
- 完全重复率保持 `0%`，但平均保留上期点位由 `1.802` 升至 `1.885/6`；候选失败点亮、逐列、近期和保留点位门槛，多重检验也不显著。完整上一年度的均匀共享规律会稀释近期状态，候选、拟合器实验参数和临时入口已完整撤回，不预注册、不搜索年度衰减或融合比例。

## 097期后的上一年度迁移逐列Hedge否决

- 在保守共识失败后，进一步测试 `annual-short-dynamic-hierarchical-previous-year-shared-transfer-column-hedge-global`：六列分别使用目标期之前最多15个同年目标期，累计当前年度模型与上一年度共享迁移模型各自在该列所选范围的Brier损失，以指数权重融合两套范围分数，再通过逐列目标的全局搜索一次性生成合法六点；当前目标期开奖结果不参与权重更新。
- 候选全年覆盖/点亮为 `3.474 / 2.959`，相对 `v5.3` 为 `+0.041 / 0`；六列差为 `+0.010 / -0.031 / +0.031 / -0.021 / 0 / +0.010`，第2、4列仍退化。最近10期为 `3.400 / 2.900`，低于影子的 `3.600 / 3.200`；最后一段点亮差为 `-0.100`。
- 完全重复率保持 `0%`，但平均保留上期点位由 `1.802` 升至 `1.823/6`；候选失败逐列和保留点位门槛，Reality Check/SPA也没有新增显著证据。逐列专家权重、专用组合器和临时研究入口已完整撤回，不搜索Hedge学习率、回看窗口或列级阈值。

## 097期后的上一年度迁移保守共识否决

- 在 `2026097` 数据截止的97期年度严格滚动研究中，测试了 `annual-short-dynamic-hierarchical-previous-year-shared-transfer-robust-global`：当前年度动态模型与只迁移2025年末共享特征系数的模型分别计算范围分数，每个范围取两者较小值，再以当前模型分数作同分判据进行六范围全局选择；没有融合权重，也未修改既有预注册平均候选。
- 候选全年覆盖/点亮为 `3.474 / 2.959`，相对 `v5.3` 为 `+0.041 / 0`；六列差为 `+0.010 / -0.031 / +0.031 / -0.031 / 0 / +0.021`，第2、4列退化。最近10期仅为 `3.300 / 2.800`，低于影子的 `3.600 / 3.200`；最后一段覆盖/点亮差为 `-0.050 / -0.150`。
- 候选完全重复率从 `0%` 升至 `1.0%`，平均保留上期点位从 `1.802` 升至 `1.885/6`，同时失败逐列、重复率和保留点位门槛。候选族覆盖/点亮 Reality Check p值为 `0.9215 / 0.8936`，SPA p值为 `0.2024 / 0.6627`，均不显著。该保守共识已撤回，不搜索下界分位数或混合比例。

## 097期后的Log-opinion候选否决

- 在 `2026097` 数据截止的97期年度严格滚动研究中，测试了 `annual-short-dynamic-hierarchical-log-opinion-global`：对基础动态模型和结构特征模型的逐球 logit 做等权平均，再还原为概率并进行原六范围全局选择；没有新增手工权重，正式版、当前 `v5.3` 影子版和开奖前密封账本均未使用该候选。
- 候选全年红球覆盖/范围点亮为 `3.433 / 3.000`，当前 `v5.3` 为 `3.433 / 2.959`；覆盖持平，点亮提高 `0.041`。但六列相对差为 `-0.031 / 0 / -0.010 / -0.010 / +0.062 / +0.031`，第1、3、4列退化，未通过逐列非劣门槛。
- 候选最近10期覆盖/点亮为 `3.500 / 3.100`，低于当前影子的 `3.600 / 3.200`；五段相对影子的覆盖差为 `-0.053 / -0.158 / +0.100 / +0.158 / -0.050`，点亮差为 `+0.105 / -0.053 / +0.050 / +0.158 / -0.050`。完全重复率虽保持 `0%`、平均保留上期点位降至 `1.594/6`，仍不能抵消六列和近期退化。
- 本轮候选族覆盖/点亮 Reality Check p值为 `0.9170 / 0.8481`，SPA p值为 `0.1989 / 0.6492`，均不显著；当前样本只有97期，也低于正式至少100个开奖前密封配对样本要求。该候选已从枚举、选择器、研究器和验证器完整撤回，不搜索 logit 融合温度或其他固定融合比例。

## 097期后的Brier Hedge候选否决

- 在 `2026097` 数据截止的97期年度严格滚动研究中，测试了 `annual-short-dynamic-hierarchical-brier-hedge-global`：只用目标期以前最多15个目标期的逐球Brier损失，在当前动态基础模型与带结构特征模型之间按表现自适应Hedge，再做六范围全局选择；正式版、当前 `v5.3` 影子版和开奖前密封账本均未使用该候选。
- 候选全年红球覆盖/范围点亮为 `3.433 / 2.990`，当前 `v5.3` 为 `3.433 / 2.959`；覆盖持平，点亮只提高 `0.031`。但六列相对差为 `-0.031 / 0 / -0.010 / -0.021 / +0.062 / +0.031`，第1、3、4列退化，未通过逐列非劣门槛。
- 候选最近10期覆盖/点亮为 `3.500 / 3.100`，低于当前影子的 `3.600 / 3.200`；五段相对影子的覆盖差为 `-0.053 / -0.158 / +0.100 / +0.158 / -0.050`，点亮差为 `+0.053 / -0.053 / +0.050 / +0.158 / -0.050`。完全重复率虽保持 `0%`、平均保留上期点位降至 `1.625/6`，仍不能抵消总体、近期和六列退化。
- 本轮候选族覆盖/点亮 Reality Check p值为 `0.9170 / 0.8876`，SPA p值为 `0.1989 / 0.6492`，均不显著；当前样本也只有97期，远低于正式至少100个开奖前密封配对样本的要求。该候选已从枚举、选择器、研究器和验证器完整撤回，不搜索损失窗口或Hedge温度。

## 当前方向（2026-08-25，年度短期）

### 097期后的新增开奖复核

- 本地开奖数据现已更新到 `2026097`，年度滚动样本为97期；`2026097` 已有真实开奖，`2026098` 已成为以 `2026097` 为信息截止点的开奖前密封记录。正式版 `position-v5.0-annual-short`、当前影子 `position-v5.3-annual-short-dynamic-hierarchical-global-shadow` 和既有密封记录均保持不变。
- `2026097` 实际开奖为 `05,16,24,26,29,30 + 02`。`v5.3` 在开奖前已密封点位 `2,8,16,22,26,31`，覆盖3个红球、点亮3个范围；预注册的 `annual-short-dynamic-hierarchical-previous-year-shared-transfer-blend05-global` 与 `v5.3` 该期逐点完全相同，也覆盖3、点亮3。该预注册候选锁定后目前只有 `1` 个样本外期，不能据此切换、改参或宣称优于影子。
- 本轮复核的当前影子平均红球覆盖/范围点亮为 `3.438 / 2.958`，完全重复率 `0%`，平均保留上期点位约 `1.789/6`。新增的 `annual-short-dynamic-hierarchical-conditioned-range-global` 与当前影子在96期上逐期选出的六个点位完全一致，覆盖、点亮和六列命中没有任何新信息，已从运行研究器和验证器撤回；通用固定6球条件概率公式仍保留为数学诊断，不再单独登记候选。
- 固定测试了方差感知共享正则 `annual-short-dynamic-hierarchical-variance-scaled-penalty-global`：保留原9项特征、30期窗口、60轮训练、学习率和总体平均正则强度，只按训练行的时间加权特征方差重新分配9个共享系数的L2惩罚。候选全年覆盖/点亮为 `3.375 / 2.885`，最近10期为 `3.4 / 3.1`；第1、3、4、5列相对 `v5.3` 分别退化 `-0.052 / -0.031 / -0.010 / -0.021`，只有第2、6列改善。候选改变 `45/96` 期，完全重复率由 `0%` 升至 `1.05%`，平均保留上期点位由 `1.789` 升至 `1.947/6`，五段中有四段覆盖退化、三段点亮退化；候选族覆盖/点亮 Reality Check p值为 `0.922 / 0.883`，SPA p值为 `0.199 / 0.655`，均不显著。该方向同时损害总体、逐列和相同化指标，已从运行研究器和验证器完整撤回，不搜索方差幂次、上下限或正则倍数。
- 固定测试了同期开奖横截面秩特征 `annual-short-dynamic-hierarchical-cross-sectional-rank-global`：每个训练目标期先计算 `v5.3` 原9项近期特征，再分别在当期33个号码内转换为并列秩，训练和预测使用同一冻结变换；窗口、训练轮数、正则、组合器均不变。候选全年覆盖/点亮降至 `3.333 / 2.844`，最近10期仅 `3.0 / 2.6`；六列相对 `v5.3` 为 `-0.073 / -0.010 / +0.021 / +0.031 / -0.042 / -0.042`，改变 `84/96` 期，完全重复率升至 `5.26%`，平均保留上期点位升至 `2.705/6`。横截面秩抹掉了热度绝对强弱和类别负载幅度，最后20期覆盖/点亮分别退化 `-0.45 / -0.35`，不适合当前短样本模型；候选已完整撤回，不继续搜索只转换部分特征或秩与原值的混合权重。
- 本轮固定测试了 `annual-short-dynamic-hierarchical-local-geometry-global`：在已有近3/7/15期频率、遗漏、重号/邻号、三区、012路、奇偶、前两期关系、质合和近三期密度特征之外，加入最新一期的 `±1` 邻号数量、`±2` 二邻号数量、是否位于最新红球跨度内、到最新红球的最小距离四项号码级几何特征。严格滚动回测的全年覆盖/点亮为 `3.354 / 2.865`，相对当前影子 `3.438 / 2.958` 分别退化 `-0.083 / -0.094`；最近10期虽为 `3.7 / 3.2`，但第1、3、6列相对影子分别退化 `-0.083 / -0.115 / -0.021`，第5列也退化 `-0.010`。候选改变 `86/96` 期，完全重复率仍为 `0%`，平均保留上期点位降至 `1.705/6`；五段覆盖/点亮为 `3.32/2.79、3.32/3.05、3.42/2.68、3.21/2.63、3.50/3.15`，并未形成稳定改善。当前候选族 Reality Check 覆盖/点亮 p 值为 `0.9295 / 0.8971`，SPA p 值为 `0.1904 / 0.6697`，且历史样本只有96期，候选未通过逐列非劣门槛，已完整撤回，不创建新影子版本，不修改正式版或 `2026097` 密封记录。
- 固定测试了删除一期重拟合的号码概率中位数 `annual-short-dynamic-hierarchical-median-jackknife-global`：每个目标期在可见近30期中逐条删除、重拟合当前9特征模型，再按号码取最多30个概率的中位数，不搜索聚合权重。全年覆盖/点亮为 `3.396 / 2.906`，最近10期为 `3.5 / 2.9`；第1、2、4列相对当前影子各退化约 `-0.021`，最差五段覆盖和点亮均为 `-0.158`。候选虽保持完全重复率 `0%`、把平均保留上期点位降到 `1.737/6`，但总体、近期和逐列质量均退化，已完整撤回；稳健中位数并不能修复近同分排序，不继续搜索截尾比例或分位数。
- 固定测试了频率对比坐标 `annual-short-dynamic-hierarchical-frequency-contrast-global`：保持9项特征数量、训练轮数和正则不变，把高度相关的近3/7/15期频率改写为“近15期水平、3对7短动量、7对15中动量”，检验共线性是否造成系数翻向。全年覆盖/点亮为 `3.417 / 2.927`，相对当前影子退化 `-0.021 / -0.031`；第1、2、4、5列分别退化 `-0.031 / -0.042 / -0.021 / -0.031`，完全重复率升至 `1.05%`。虽然最近10期覆盖/点亮持平 `3.6 / 3.1`、第6列全年提升 `+0.063`，但最差分段覆盖/点亮为 `-0.211 / -0.158`，未通过逐列和重复率门槛，已完整撤回；不继续搜索动量缩放或不同正则强度。
- 随后用删除一期集成的自然不确定性测试 `annual-short-dynamic-hierarchical-jackknife-one-se-global`：先比较当前组合与删除一期均值概率组合在每个删除样本上的总分差，只有平均优势严格超过一个标准误差才允许切换，不设人工倍数。该闸门在 `96/96` 期全部保留当前影子，全年、近期、六列、五段、重复率和保留点位完全相同。说明此前 jackknife 均值候选的组合分数变化均不足以超过自身删除样本噪声，没有可用于预测的新信息；高开销候选已完整撤回，不搜索标准误差倍数。
- 固定测试了严格开奖前的点位中心残差校准 `annual-short-dynamic-hierarchical-point-center-residual-global`：对当前期之前最多30个同年目标期逐期重建当时 `v5.3` 范围分数，以实际点亮减预测分数的残差投影到中心位置线性项和中心化二次项，再校正当前范围排序；没有读取目标期开奖结果，也不设置融合权重。候选覆盖/点亮仅 `3.354 / 2.813`，最近10期为 `3.3 / 2.9`；第1至第3列相对影子各退化 `-0.052`，完全重复率升至 `9.47%`，平均保留上期点位升至 `3.737/6`，最后20期覆盖/点亮差为 `-0.50 / -0.35`。同一期31个重叠范围事件高度相关，直接池化会严重低估位置残差不确定性并造成组合黏滞，原始校准已撤回，不搜索融合比例。
- 对同一方向追加了参数自由的按期 one-SE 闸门 `annual-short-dynamic-hierarchical-point-center-residual-one-se-global`：先在每个已开奖目标期分别估计线性和二次中心残差系数，只有跨期均值绝对值超过一个跨期标准误差时才启用。全年覆盖微升到 `3.448`，但点亮降到 `2.917`，最近10期降到 `3.4 / 2.9`；第1、2、6列分别退化 `-0.021 / -0.031 / -0.073`，完全重复率升至 `12.63%`，平均保留上期点位升至 `3.242/6`，最后20期覆盖/点亮差为 `-0.30 / -0.25`。宽泛的中心位置偏差会把组合长期拉向固定区域，直接违背逐列非劣和减少相同化目标；该候选已完整撤回，停止中心多项式残差校准方向。
- 固定测试了组合级统计等价多样性 `annual-short-dynamic-hierarchical-equivalent-diversity-global`：先按当前 `v5.3` 找最优六点组合，再用近30期删除一期重拟合得到候选相对最优组合的动态分数标准误差，只在分数差不超过一个标准误差的组合中最小化与上一期 `v5.3` 点位的重合数；随后又增加当前范围事件分与近30期逐期点亮差的第二个one-SE等价约束。初版把平均保留点位从 `1.789` 降到 `1.484/6`，双约束版为 `1.526/6`，两版完全重复率均为 `0%`、全年覆盖均与影子持平 `3.438`；但点亮均降到 `2.927`，最近10期均降到 `3.5 / 3.0`。双约束版第1、2、4、6列仍分别退化 `-0.021 / -0.010 / -0.010 / -0.010`，最差分段覆盖/点亮为 `-0.053 / -0.105`。统计不可区分只表示差异证据不足，并不证明候选非劣，不能作为牺牲当前最优分数换多样性的许可；候选及高开销实现已完整撤回。后续多样性只允许用于主分和范围事件次分均精确同分的组合，不再使用one-SE等价集合。
- 固定测试了训练期滚动特征标准化 `annual-short-dynamic-hierarchical-standardized-global`：每个目标期只使用训练行的时间权重计算9项特征均值和标准差，对训练行与预测行应用同一冻结变换，模型特征、窗口、60轮训练、共享正则和号码效应正则均不变。候选全年覆盖/点亮为 `3.375 / 2.927`，相对当前影子退化 `-0.063 / -0.031`；第1、3、5列分别退化 `-0.063 / -0.052 / -0.052`，平均保留上期点位升至 `1.842/6`，最差分段覆盖/点亮为 `-0.263 / -0.211`。最近10期虽升至 `3.9 / 3.5`，最后20期也为 `+0.05 / +0.10`，但这是已观察到的阶段结果，不能据此事后创建切换窗口；候选改变 `69/96` 期，全年与逐列门槛明确失败，已完整撤回。该结果只说明标准化模型可能是未来预注册专家，不修改当前影子、不按最近10期回看切换。
- 随后固定测试严格开奖前的标准化专家选择 `annual-short-dynamic-hierarchical-standardized-champion-global`：每个目标期重建此前最多30个同年目标期的基础/标准化开奖前预测，只有标准化模型逐期点亮优势的配对均值超过一个配对标准误差时才切换，否则使用 `v5.3`；不读取当前期开奖结果，也不设置窗口或阈值倍数。候选在 `96` 期中只切换 `1` 期，而该次实际少覆盖、少点亮各1，使全年覆盖/点亮降到 `3.427 / 2.948`，最近10期降到 `3.5 / 3.0`，第5列退化 `-0.010`；平均保留点位略降至 `1.779/6`、完全重复率仍为0，但不能抵消预测退化。标准化模型的近期优势出现得太晚，无法被此前有限样本可靠识别；候选与标准化运行实现均已完整撤回，不按已知最近结果改窗口或门槛。
- 固定测试了组合级 `annual-short-dynamic-hierarchical-all-ranges-lit-global`：在动态号码概率转换的恰好6球加权无放回分布下，因六个范围互不重叠且开奖正好6球，“六个范围全部点亮”精确等价于六个范围权重和的乘积除以六阶基本对称和。96期覆盖/点亮为 `3.448 / 2.958`，相对当前影子覆盖 `+0.010`、点亮持平，六列命中全部持平，最近10期仍为 `3.6 / 3.1`；但完全重复率升至 `1.05%`，平均保留上期点位升至 `1.821/6`，逐列与稳定性门槛未通过。候选已完整撤回，不创建 `v5.4`，不改当前影子或 `2026097` 密封记录。
- 固定测试了概率层 logit 温度平滑 `annual-short-dynamic-hierarchical-temperature2-global`：把 v5.3 的每球概率按固定 `T=2` 向中间收缩后再做原六范围全局选择，不搜索温度。96期覆盖/点亮为 `3.448 / 2.948`，相对当前影子为 `+0.010 / -0.010`；六列差值为 `-0.021 / -0.010 / +0.010 / -0.010 / +0.010 / +0.010`，完全重复率保持 `0%`，但第1、2、4列退化且点亮下降，候选已撤回，不继续搜索温度参数。
- 固定测试了动态号码权重下的精确有序列概率 `annual-short-dynamic-hierarchical-exact-order-global`：用六阶基本对称和精确计算第 k 个有序开奖号落入各点位范围的概率，并按列联合选六点，不使用历史开奖结果做列校准。96期覆盖/点亮仅 `3.198 / 2.781`，相对当前影子退化 `-0.240 / -0.177`；最近10期覆盖/点亮为 `2.8 / 2.5`，完全重复率 `1.05%`，平均保留上期点位 `3.695/6`。该方向明显产生组合黏滞，已完整撤回，不再沿理论顺序统计量扩展。
- 现有 `annual-short-dynamic-conditional-hierarchical-range-global` 的平均红球覆盖约 `3.469`、范围点亮约 `2.969`，但第5列相对当前影子为 `-0.010`，且六列非劣门槛未通过；它只能作为研究对照，不能进入影子版。新增数据不足100期，也不足以支持正式晋升或针对第5列反向调参。
- 本轮没有满足“总覆盖提升、六列均不退化、重复率不升高、保留点位不变差”四项约束的新方法，因此不创建 `v5.4`，不修改当前影子，不补写或重算已结算的 `2026097` 密封预测。
- 前向账本现已结算 `2026097` 并密封 `2026098`：`2026097` 实际开奖为 `05,16,24,26,29,30 + 02`；`v5.3` 点位命中3、点亮3。`2026098` 的 `v5.3` 开奖前点位固定为 `2,5,8,12,16,20`，预测哈希为 `AC5B33A90A90858BB39B42905C5CE27519C1E34D5AE910D793CBAFD6648631E8`，仍未结算。该样本外窗口仍远低于至少100期的开奖前密封样本门槛，不能据此调参。
- 本轮追加测试了两条不调权重的新方向并全部撤回：其一是用动态分层概率直接计算第1至第6个顺序统计量，再用间距约束动态规划选点，96期覆盖/点亮降至 `3.198 / 2.781`，第1列相对影子为 `-0.083`，平均保留上期点位升至 `3.695/6`；其二是从当前影子、条件似然和稳定混合三个固定专家中按此前各列命中率逐列选点，覆盖/点亮为 `3.479 / 2.927`，第3/5列分别为 `-0.021 / -0.052`，完全重复率升至 `1.05%`。两者都没有进入运行实现，也不继续在96期样本上调顺序或切换阈值。
- 随后测试的独立边际概率与固定6球条件似然概率等权集成只改变 `2/96` 期，覆盖/点亮为 `3.458 / 2.969`，第5列相对影子为 `-0.010`，仍未通过逐列门槛，已撤回；下一条实验转向只用历史预测残差做逐期概率校准，不混合结果权重。
- 逐球历史残差校准实验把此前动态概率的实际减预测残差直接累加到下一期概率，未增加收缩参数；结果覆盖/点亮降至 `3.208 / 2.792`，完全重复率升至 `12.63%`，第1至第3列相对影子为 `-0.073 / -0.052 / -0.104`，证明年度短窗口不能承受无收缩残差纠偏。该候选已撤回，不继续在同一96期上搜索收缩系数。
- 最后测试了六列输出中心的历史偏差校准：只用此前每期预测中心与实际第k个红球的均值偏差修正下一期中心，并固定约束范围不重叠。覆盖/点亮为 `3.344 / 2.865`，第1/2/3/5列相对影子为 `-0.021 / -0.021 / -0.052 / -0.021`，虽完全重复率仍为 `0%`，但总体和逐列命中均退化，已撤回；不继续调整平移取整或边界规则。
- 原始测试了允许六个 `±1` 范围重叠的直接取六高分范围方案：第4列相对提升 `+0.083`，但第5列退化 `-0.104`，18码唯一覆盖降至 `2.260`，完全重复率虽为 `0%`，却不满足红球覆盖和逐列门槛。该实验确认“允许重叠”只能作为数据结构能力保留，不能直接作为当前点位推荐策略。
- 在此基础上又测试了“保持当前动态18码覆盖在一个历史标准误差内、只用顺序统计概率作二级选择”的保守版本；覆盖/点亮仍降至 `3.219 / 2.792`，第1列为 `-0.083`，完全重复率升至 `1.05%`，平均保留上期点位 `3.684/6`。这证明顺序位置优化即使被限制为二级目标仍会破坏当前组合，相关路径停止扩展并已撤回。
- 随后追加测试了 `annual-short-dynamic-previous-year-prior-global`：当前动态分层模型仅把2025年末最多30期频率作为约3%的弱先验，未搜索权重。96期平均覆盖/点亮为 `3.427 / 2.906`，相对当前影子分别为 `-0.010 / -0.052`；六列差值为 `-0.052 / -0.021 / +0.010 / -0.052 / +0.021 / +0.042`，完全重复率仍为 `0%`，平均保留上期点位 `1.979/6`。虽然第3、5、6列略有改善，但总覆盖、范围点亮及第1、2、4列退化，未通过非劣门槛，候选已从运行研究器和验证器撤回；不继续在96期样本上调整上一年度先验比例。
- 本轮一次性测试了号码级结构类比后验 `annual-short-dynamic-analog-ball-global`：在目标期前最近30期内寻找最近8个三期开奖结构上下文，用其已发生的下一期开奖形成号码后验，再以当前动态模型作为12期等效先验收缩。该候选平均覆盖/点亮为 `3.271 / 2.802`，相对当前影子退化 `-0.167 / -0.156`；六列差值为 `-0.063 / -0.052 / -0.021 / -0.042 / +0.021 / 0`，完全重复率仍为 `0%`，但平均保留上期点位升至 `2.274/6`。候选明显失败，已从运行研究器和验证器完整撤回；不继续搜索相似度、邻居数或收缩比例。
- 随后一次性测试了固定学习率衰减的在线分层逻辑模型 `annual-short-dynamic-online-hierarchical-ball-global`：按时间顺序逐期开奖更新共享特征和号码效应，预测下一期时只使用已经发生的更新，不增加窗口或权重搜索。该候选平均覆盖/点亮为 `3.250 / 2.813`，相对当前影子退化 `-0.188 / -0.146`；六列差值为 `-0.052 / -0.010 / -0.021 / -0.010 / -0.052 / 0`，完全重复率 `0%`，平均保留上期点位 `2.084/6`。六列没有提升，候选已从运行研究器和验证器完整撤回；不继续搜索学习率、衰减速度或在线更新次数。
- 本轮最后测试了 `annual-short-dynamic-hierarchical-model-champion-global`：每个目标期只依据此前12个目标期的范围事件 Brier 误差，在当前动态模型和结构集成模型之间选择整体专家，不按列使用开奖结果切换。候选覆盖/点亮为 `3.427 / 2.990`，相对当前影子为 `-0.010 / +0.031`；六列差值为 `-0.042 / +0.031 / -0.031 / 0 / +0.031 / +0.042`，完全重复率 `0%`，平均保留上期点位降至 `1.653/6`。虽然点亮和相同化指标改善，但总覆盖及第1、3列退化，未通过逐列非劣门槛，候选已完整撤回；不继续搜索专家回看窗口或切换规则。
- 本轮测试了范围事件两两共现模型 `annual-short-dynamic-hierarchical-pairwise-range-global`：以动态号码概率得到节点分数，再用近30期范围共同点亮的收缩对数比作为边分数，联合选择六个相互间隔至少3的点位。候选覆盖/点亮为 `3.271 / 2.750`，相对当前影子退化 `-0.167 / -0.208`；六列差值为 `-0.135 / +0.010 / -0.104 / +0.021 / 0 / 0`，相邻完全重复率升至 `45.3%`，平均保留上期点位 `4.168/6`。共现项导致组合黏滞，候选已从运行研究器和验证器完整撤回；不继续搜索边权或共现先验。
- 随后测试了直接预测31个范围事件的层级逻辑模型 `annual-short-dynamic-hierarchical-range-event-model-global`：用每个目标期之前可见的近30/15/5期点亮率、近期状态、遗漏、状态持续和动量特征，按时间滚动训练共享系数与点位效应。候选覆盖/点亮为 `3.115 / 2.615`，相对当前影子退化 `-0.323 / -0.344`；六列差值为 `-0.208 / +0.010 / -0.094 / +0.021 / -0.073 / 0`，完全重复率 `5.3%`，平均保留上期点位 `2.526/6`。直接事件模型明显过度收缩并损害整体组合，已从运行研究器和验证器完整撤回；不继续搜索其学习率、正则或点位效应强度。
- 本轮又测试了受当前动态六点限制的局部列校准 `annual-short-dynamic-hierarchical-local-column-calibration-global`：每列只允许在当前基准点 `-1/0/+1` 邻域内变化，以相同列历史顺序红球命中率和固定30期先验重新评分。候选覆盖/点亮为 `3.354 / 2.896`，相对当前影子退化 `-0.083 / -0.063`；六列差值为 `-0.052 / -0.010 / -0.010 / +0.010 / 0 / 0`，完全重复率 `2.1%`，平均保留上期点位 `2.074/6`。局部约束没有阻止第1至第3列退化，候选已完整撤回；不继续搜索邻域半径或先验强度。
- 随后测试了号码级固定交互特征模型 `annual-short-dynamic-hierarchical-interaction-ball-global`：在现有形状特征之外加入同一区负载与重号/邻号、同路负载与重号、奇偶负载与重号、局部密度与邻号的乘积项，参数固定且严格滚动训练。候选覆盖/点亮为 `3.427 / 2.969`，相对当前影子为 `-0.010 / +0.010`；六列差值为 `-0.021 / -0.031 / -0.042 / +0.063 / 0 / +0.042`，完全重复率 `0%`，平均保留上期点位 `1.600/6`。虽然第4、6列和点亮有所改善，但第1至第3列退化，未通过逐列非劣门槛，候选已完整撤回；不继续搜索交互项或正则强度。
- 固定测试了把动态号码概率转换为独立三球范围事件概率 `1-Π(1-p)` 后再全局选点的候选；它只改变 `1/96` 期，覆盖、点亮、六列命中、完全重复率和平均保留点位与当前影子完全相同。该变换在当前分数区间没有新增排序信息，已作为冗余候选完整撤回，不继续变换范围效用。
- 随后测试了无权重的稳健共识下界 `annual-short-dynamic-hierarchical-robust-consensus-global`：每个范围取基础动态模型与形状模型范围分数的较小值，避免平均集成以单侧高分补偿另一侧不确定性。候选覆盖/点亮为 `3.448 / 2.958`，相对当前影子为 `+0.010 / 0`；六列差值为 `+0.010 / -0.010 / -0.042 / +0.031 / +0.010 / 0`，完全重复率 `0%`，平均保留上期点位降至 `1.653/6`。总覆盖和第1/4/5列有所改善，但第2、3列退化，未通过逐列非劣门槛，候选已完整撤回；不根据本次已观察列差临时拼接前后列模型。
- 本轮补齐固定6球条件似然与形状特征的自然组合 `annual-short-dynamic-conditional-hierarchical-shape-range-global`，没有新增参数搜索。其原始范围概率 Brier 为 `0.248477`、优于随机基线 `0.248707`，AUC为 `0.5288`，前后半段为 `0.5250 / 0.5325`；但六范围全局选择后的覆盖/点亮仅 `3.344 / 2.927`，相对当前影子退化 `-0.094 / -0.031`。六列差值为 `-0.052 / -0.010 / -0.052 / +0.031 / 0 / +0.052`，完全重复率 `0%`，平均保留上期点位 `1.568/6`。概率排序质量没有转化为受约束组合命中，第1、3列明显退化，候选及其诊断已从运行研究器和验证器完整撤回。

- 新增10期 `2026087-2026096` 已作为固定的最近10期窗口单独审计，不改变年度主门槛。`v5.3` 在这10期平均覆盖 `3.6`、范围点亮 `3.1`，六列点亮率为 `60% / 50% / 60% / 70% / 40% / 30%`；条件似然候选在这10期与 `v5.3` 完全一致，5%稳定信号混合则覆盖少 `0.1`、点亮多 `0.1`，且全年逐列与重复率门槛仍失败。正式版、当前影子和密封账本保持不变。
- 为检验概率排序与全局组合目标是否失配，预先固定测试了“每期固定开6球条件下，最大化18个覆盖号码至少命中4球概率”的全局目标，并用条件期望覆盖作唯一同分判据。该目标在96期中与 `v5.3` 的点位 `96/96` 完全相同，全年与最近10期的覆盖、点亮、六列、重复率和平均保留点位均无任何变化，证明当前约束下这个尾概率目标没有新增排序信息；候选已从枚举、选择器、研究配置和验证器完整撤回，不继续搜索命中阈值。
- 随后按点位定义放开“六个范围必须互不重叠”的限制，固定测试 `annual-short-dynamic-hierarchical-overlap-coverage-global`：31个合法中心可任选6个，主目标最大化范围并集内的固定6球条件期望覆盖，二级目标最大化六范围点亮概率，不设置重叠惩罚或阈值。搜索空间由约八千组扩大到约七十三万组，但96期最优点位仍逐期与 `v5.3` 完全相同，算法没有一期选择重叠范围。由此确认当前瓶颈不在非重叠组合约束，而在号码概率信号；候选已完整撤回，后续不再围绕重叠距离搜索参数。
- 转向号码概率后，固定消融了动态分层模型中的33个号码身份效应，只保留近3/7/15期频率、遗漏、重号/邻号、三区、012路与奇偶负载。原始范围概率 Brier 由 `0.248852` 略退至 `0.248854`，AUC由 `0.51908` 微升至 `0.51914`；组合后覆盖/点亮为 `3.448 / 2.969`，相对 `v5.3` 各 `+0.010`，最近10期为 `3.8 / 3.2`。但第1、2列分别退化 `-0.010 / -0.021`，完全重复率由 `0%` 升到 `1.05%`，最差分段覆盖差为 `-0.105`，未通过逐列与相同化门槛。候选及概率诊断已完整撤回，不继续搜索号码效应惩罚强度。
- 固定测试了同一动态分层正则目标的收敛求解：特征、窗口、学习率、正则和组合器不变，至少运行原60轮，之后仅按参数更新数值容差继续迭代。其原始Brier由 `0.248852` 退至 `0.248857`，AUC由 `0.51908` 降至 `0.51813`；组合覆盖升至 `3.469`、点亮保持 `2.958`，最近10期为 `3.7 / 3.2`，但第1、2、5列各退化 `-0.010`，完全重复率升到 `1.05%`，最差分段覆盖和点亮均为 `-0.053`。这表明固定60轮的未完全收敛具有早停正则效果；候选及诊断已完整撤回，不在当前96期上搜索最佳训练轮数。
- 年度研究器新增9个共享动态特征的严格滚动系数稳定性诊断；每个目标期只用开奖前数据拟合，并报告前后半段、五段均值、正号比例和同号一致率，未来开奖篡改回归覆盖该诊断。96期结果显示：近3/7/15期频率前三至四段为正、最后20期转负；遗漏前四段为负、最后20期转正；重号效应除首段外持续为正，同号率 `85.4%`；邻号效应前后半段由负转正、同号率仅 `51.0%`；三区负载前两段为正、后三段稳定为负；012路负载前四段主要为正、最后一段转负；奇偶负载多次翻向。当前模型失效主要来自阶段关系改变，而非单一固定坏特征；不按这96期结果事后删除或拼接特征，后续候选必须在目标期内用过去系数稳定性自适应并接受独立前向验证。
- 基于上述诊断，固定测试了参数自由的历史符号稳定收缩：目标期内用可见近30期的多个时间前缀重拟合系数，以 `|正期数-负期数|/有效期数` 连续收缩当前共享系数，稳定特征接近保留、翻向特征接近零，不使用人工名单或阈值。结果原始Brier退至 `0.248976`、AUC降至 `0.51433`；覆盖/点亮降到 `3.344 / 2.875`，最近10期仅 `3.2 / 2.8`，第1至第5列均退化，后半段相对 `v5.3` 为 `-0.167 / -0.188`。阶段翻向刚发生时，历史稳定性会把新方向压回零并加重适应滞后；候选及诊断已完整撤回，不继续搜索稳定性阈值或回看长度。
- 随后固定测试等分时间窗的零交叉阶段切换：将可见30期等分为前后15期，某特征前后系数异号时采用后15期系数，否则保留完整30期系数；不设显著性阈值或特征名单。原始Brier退至 `0.249029`、AUC降至 `0.51210`；覆盖/点亮仅 `3.271 / 2.771`，最近10期为 `3.1 / 2.8`，第1至第5列均退化，后半段相对 `v5.3` 为 `-0.271 / -0.292`，最后一期少覆盖和少点亮各2球。15期子模型的异号主要反映小样本估计波动，不足以确认变点；候选及诊断已完整撤回，不搜索分段比例或零交叉阈值。
- 年度研究器进一步加入动态模型预quential残差的单变点BIC诊断，逐期审计平均校准残差、Brier损失和期内AUC；每个历史时点只用截至该期的残差序列，在单均值模型与一个未知分割点的双均值模型间由BIC自动惩罚复杂度。96期内校准残差仅在 `2026008-2026010、2026015` 报警，Brier仅在 `2026008-2026010` 报警，AUC在 `2026004、2026010、2026074-2026075` 报警；三项最近10期均未报警。当前三项BIC优势分别为 `-5.52 / -5.31 / -5.74`，均明确支持无变点，说明系数方向翻转尚未形成可实时确认的整体预测性能变点。诊断保留并纳入未来隔离回归，但不据此创建重置、缩窗或切换候选。
- 为降低30期小样本对单期开奖结果的敏感性，固定测试了delete-one jackknife概率集成：逐条删除可见历史、重拟合动态分层模型并等权平均33球概率，不设窗口或集成权重。组合覆盖/点亮升至 `3.490 / 2.979`，前后半段覆盖优势均为正，完全重复率仍为0；但原始Brier由 `0.248852` 退至 `0.248861`、AUC由 `0.51908` 降至 `0.51768`，第1、2列各退化 `-0.021`，最近10期相对 `v5.3` 为 `-0.1 / -0.1`，平均保留上期点位升至 `1.884/6`，单次研究耗时约50秒。总均值改善没有转化为逐列、近期或相同化改善，候选及诊断已完整撤回，不继续搜索删除块大小或集成权重。
- 固定测试了号码级成对排序目标 `annual-short-dynamic-hierarchical-pairwise-ranking-ball-global`：保持30期窗口、9项特征、60轮训练和既有正则不变，以每期6个中奖球相对27个未中奖球的成对排序损失替代独立二分类损失，并按固定6球条件概率输出。候选全年覆盖/点亮为 `3.458 / 2.948`，最近10期为 `3.7 / 3.2`，第6列相对 `v5.3` 提升 `+0.063`；但第1/2/5列分别退化 `-0.042 / -0.010 / -0.021`，全年点亮退化 `-0.010`，完全重复率从 `0%` 升至 `1.05%`，最差分段覆盖和点亮均为 `-0.105`。Reality Check与SPA仍不显著，候选已从枚举、研究配置、选择器和验证器完整撤回，不继续搜索成对损失权重或正则；仅保留恰好5期历史时返回有限合法概率的边界回归。
- 新增当前 `v5.3` 六列已选范围的严格前向概率可靠性诊断：六列平均都位于同期31个候选范围的前约20%-25%，但跨期AUC依次仅为 `0.439 / 0.444 / 0.481 / 0.582 / 0.579 / 0.512`，Brier skill依次为 `-0.55% / -0.46% / -0.35% / +0.40% / +0.53% / +0.40%`。第1至第3列不是全局间距约束把它们挤到低分，而是当前动态概率在这些列对命中与未命中的排序缺乏区分力。各列漏点中 `43.6%-54.4%` 在固定其他五列时存在合法可命中替代点，模型错选范围相对最佳可命中替代范围的平均分差仅 `0.0135-0.0242`，说明主要瓶颈是近同分候选的概率排序噪声。该诊断保留，不参与预测。
- 基于近同分诊断固定测试 `annual-short-dynamic-hierarchical-temporal-robust-global`：同一动态模型分别使用当前近30期和去掉最新一期的历史评分，每个范围取两次分数较小值后全局选点，不搜索混合权重。候选覆盖/点亮降至 `3.417 / 2.875`，最近10期为 `3.3 / 3.0`；第6列虽提升 `+0.094`，第1/2/3/5列分别退化 `-0.042 / -0.052 / -0.063 / -0.021`，完全重复率升至 `1.05%`，平均保留上期点位升至 `2.453/6`，最差分段两项均为 `-0.35`。时间下界加重组合黏滞且没有修复前三列反排序，候选已完整撤回，不搜索删除期数或聚合分位数。
- 固定测试了三区层级截距 `annual-short-dynamic-hierarchical-zone-intercept-ball-global`：在现有33个号码身份效应之外增加3个共享三区基准效应，沿用共享特征正则，不增加窗口或权重。候选覆盖/点亮降至 `3.250 / 2.729`，最近10期仅 `2.8 / 2.4`，六列相对 `v5.3` 全部退化，完全重复率升至 `1.05%`，平均保留上期点位升至 `2.653/6`。三区总量校准无法解决区内近同分排序，候选已完整撤回；不继续搜索三区截距正则。
- 固定测试了区内成对排序 `annual-short-dynamic-hierarchical-within-zone-ranking-ball-global`：保持当前 `v5.3` 每个区的总概率质量，只用同一区中奖号码与未中奖号码的成对损失重排区内号码，不增加窗口或混合权重。候选覆盖/点亮为 `3.375 / 2.844`，最近10期为 `3.5 / 3.2`；第4、5列相对 `v5.3` 提升 `+0.010 / +0.021`，但第1/2/3/6列分别退化 `-0.042 / -0.052 / -0.031 / -0.021`，平均保留上期点位也略升至 `1.832/6`。局部排序目标仍无法从现有9项特征中提取前三列信号，候选已完整撤回，不搜索局部损失权重或分区方式。
- 复用既有one-SE组合器测试 `annual-short-dynamic-hierarchical-stable-signal-one-se-global`：动态分数限定可接受组合，五段AUC均高于0.5的稳定动量/中点回避信号只作二级判据，不设置融合权重。候选却改变 `96/96` 期，说明按历史点亮率计算的标准误差与动态组合分数量纲不匹配、允许集合过宽；覆盖/点亮降至 `3.313 / 2.813`，最近10期仅 `2.9 / 2.4`。第6列虽提升 `+0.146`，第1/2/3/5列分别退化 `-0.125 / -0.042 / -0.094 / -0.083`，平均保留上期点位升至 `2.537/6`。候选已完整撤回，不在当前96期上搜索容差倍数或融合比例。
- 按“2025只提供共享规律、当前近30期决定号码”固定测试 `annual-short-dynamic-hierarchical-previous-year-shared-transfer-global`：用目标年前一年末30期预训练9项共享特征系数，再以当前年度近30期完整微调60轮，33个号码效应从零开始，不迁移上一年号码频率。候选覆盖升至 `3.479`、点亮持平 `2.958`，第1/3/6列分别提升 `+0.010 / +0.031 / +0.021`；但第2、4列各退化 `-0.031`，最近10期覆盖/点亮少 `0.2 / 0.3`，后半段覆盖转为 `-0.021`，完全重复率升至 `1.05%`，平均保留点位升至 `1.874/6`，未通过门槛。
- 随后固定测试当前 `v5.3` 与迁移模型的等权号码概率平均，不搜索比例。它只改变 `10/96` 期，覆盖仍为 `3.479`、点亮持平，完全重复率保持 `0%`，前后半段覆盖优势为 `+0.063 / +0.021`，最差分段覆盖为0；但第2列仍退化 `-0.031`、第4列退化 `-0.021`，最近10期覆盖/点亮少 `0.1 / 0.2`，最差分段点亮为 `-0.1`，平均保留点位略升至 `1.811/6`。两条迁移候选均已完整撤回，不在同一96期上搜索迁移轮数、先验强度或模型平均比例；该等权版本作为下一批未观察开奖到来后优先重测的预注册方向。
- 固定测试了组合级精确目标 `annual-short-dynamic-hierarchical-at-least-three-lit-global`：在每期恰好开6球的条件泊松分布下，用状态动态规划精确计算六个非重叠范围中至少3个不同范围点亮的联合概率，范围外号码和单范围多球均纳入计算；阈值3由当前平均点亮约2.96自然确定，不搜索阈值或权重。该目标在 `95/96` 期与 `v5.3` 完全相同，唯一不同期的实际覆盖、点亮和六列命中也相同，因此全年、最近10期、六列、分段、重复率和平均保留点位均无任何变化。点亮尾概率与当前期望覆盖在现有分布下几乎同序，候选及高开销动态规划已完整撤回，不继续搜索点亮阈值。
- 固定测试了号码概率的结构边际校准 `annual-short-dynamic-hierarchical-structure-marginal-global`：先由当前动态模型产生33球概率，再只用目标期前近30期中与最新一期三区比、012路比和奇偶比完全相同的历史状态，收缩估计下一期各组数量并按比例校准号码概率，不搜索窗口、权重或阈值。候选覆盖/点亮仅 `3.260 / 2.729`，相对 `v5.3` 退化 `-0.177 / -0.229`；六列差为 `-0.083 / +0.010 / -0.052 / -0.042 / -0.031 / -0.031`，最近10期少 `0.2 / 0.1`，完全重复率由 `0%` 升至 `3.16%`，平均保留点位升至 `1.821/6`。精确结构状态在30期内过于稀疏，边际校准会把噪声放大到全部号码；候选已完整撤回，不搜索状态距离、收缩强度或校准顺序。
- 预注册候选 `annual-short-dynamic-hierarchical-previous-year-shared-transfer-blend05-global` 已恢复为可持续、只读的研究定义：2025年末30期只预训练9项共享特征系数，当前年度近30期仍完整训练号码效应，再与 `v5.3` 号码概率固定等权平均。它只进入年度滚动报告和 Reality Check/SPA 候选族，不创建正式或影子预测器、不写前向密封账本、不出现在点位UI；参数锁定信息点为 `2026096`，首个可计样本外目标为 `2026097`，报告单独输出锁定后样本数、覆盖/点亮差及六列差。当前锁定后样本数为 `1`：`2026097` 与 `v5.3` 逐点相同，覆盖/点亮差均为0；后续开奖只追加样本外结果，不回看调整迁移轮数、先验强度或模型比例。97期既有回测仍仅是开发证据，不能作为晋升依据。
- 纳入 `2026097` 后，多重检验候选数为55；红球覆盖 Reality Check/SPA p值为 `0.9170 / 0.1989`，范围点亮为 `0.8876 / 0.6492`，均未通过5%门槛。预注册候选的第一个样本外结果没有提供切换证据。
- 固定测试了号码级关系特征 `annual-short-dynamic-hierarchical-relation-ball-global`：在当前9项动态特征之外一次性加入上期同尾数量、`34-号码` 对称号和上期两码整数中点三项，由滚动逻辑模型自行学习正负方向，不搜索特征子集或权重。原始范围概率 Brier/AUC 为 `0.248813 / 0.51694`，均弱于 `v5.3` 的 `0.248852 / 0.51908`；组合覆盖/点亮为 `3.417 / 2.927`，相对退化 `-0.021 / -0.031`，六列差为 `-0.010 / -0.042 / -0.010 / -0.052 / +0.031 / +0.052`，最近10期少 `0.3 / 0.2`，后半段覆盖差 `-0.229`。候选虽把平均保留点位降至 `1.779/6`，但预测质量和前四列明显退化，已完整撤回；不根据已观察结果拆分搜索同尾、对称或中点子集。
- 固定测试了遗漏非线性候选 `annual-short-dynamic-hierarchical-omission-curvature-ball-global`：保留原遗漏线性项并增加其平方项，让同一滚动逻辑模型无阈值学习U形或倒U形关系，不搜索分箱、拐点或多项式阶数。原始范围概率 Brier/AUC 为 `0.248849 / 0.51810`，没有优于 `v5.3` 的 `0.248852 / 0.51908`；组合覆盖升到 `3.458`、点亮持平 `2.958`，但六列差为 `+0.010 / -0.031 / +0.031 / -0.021 / +0.010 / 0`，最近10期覆盖/点亮各少 `0.2`，最差分段点亮为 `-0.05`，平均保留点位升至 `1.832/6`。候选只改变 `8/96` 期，局部覆盖改善不足以抵消第2/4列、近期和相同化退化，已完整撤回；不继续搜索遗漏分箱、幂次或手工冷热阈值。
- 固定测试了时间地平线等权集成 `annual-short-dynamic-hierarchical-horizon-ensemble-global`：同一动态分层模型分别使用最近30期和最近15期拟合，两个33球概率固定等权后进行六范围全局选择，不搜索窗口或混合比例。原始范围概率 Brier/AUC 为 `0.249640 / 0.50598`，显著弱于 `v5.3` 的 `0.248852 / 0.51908`；组合覆盖/点亮为 `3.375 / 2.854`，相对退化 `-0.063 / -0.104`，最近10期少 `0.4 / 0.5`，第2列退化 `-6.25` 个百分点，后半段覆盖差 `-0.167`，完全重复率虽为 `0%` 但不能弥补预测退化。15期拟合方差过大，候选已完整撤回，不再搜索短期地平线平均或替换比例。
- 固定测试了正部 James-Stein 经验贝叶斯号码效应收缩 `annual-short-dynamic-hierarchical-empirical-bayes-ball-global`：根据每个号码效应的估计能量和滚动信息量，自动计算无人工强度参数的正部收缩因子，再做六范围全局选择。原始范围概率 AUC 从 `0.51908` 微升至 `0.51931`，但Brier为 `0.248855`；组合覆盖/点亮与 `v5.3` 均为 `3.438 / 2.958`，六列差为 `-1.04 / -2.08 / +2.08 / -1.04 / 0 / +2.08` 个百分点，最近10期覆盖/点亮为 `3.7 / 3.1`，完全重复率升至 `1.05%`，平均保留点位降至 `1.737/6`。概率层的微小改善没有转化为六列非劣，且相同化门槛失败；候选已完整撤回，不搜索收缩强度、先验方差或替代估计量。
- 固定测试组合层几何均值目标 `annual-short-dynamic-hierarchical-geometric-mean-global`：保持 `v5.3` 单球概率不变，把六个范围分数的总和改为六项分数几何均值，仍要求六个范围互不重叠，不增加权重或阈值。候选覆盖/点亮为 `3.469 / 2.958`，相对 `v5.3` 提升 `+0.031 / 0`；第1列退化 `-1.04` 个百分点，第2至第5列持平，第6列提升 `+1.04` 个百分点，最近10期覆盖/点亮完全持平；但完全重复率升至 `1.05%`，平均保留点位升至 `1.832/6`。候选未通过逐列、重复率和保留点位门槛，已完整撤回，不按已知第1列差异拼接列专用组合器。

### 091期后的六列目标审计

- 本地开奖数据已更新到 `2026091`。`2026090` 和 `2026091` 的正式版、`v5.3` 影子版记录均已结算；`2026092` 仍是开奖前密封记录，不因研究结果改写。
- 截至091的91期年度滚动研究中，`v5.3` 红球覆盖/范围点亮均值为 `3.451 / 2.967`，相邻完全重复率为 `0%`，六列提升依次为 `+0.130 / +0.042 / +0.042 / -0.035 / +0.064 / -0.057`。091单期影子版覆盖6个红球，但样本仍不足晋升门槛。
- 预注册的固定条件概率候选平均为 `3.484 / 2.978`，最弱列为 `-0.046`，相对 `v5.3` 为 `+0.033 / +0.011`，前后半段优势为 `+0.044 / +0.022`；但相同率为 `1.1%`，六列非劣门槛和相同率门槛均未通过，暂不进入前向验证。
- 晋级门槛已补全：候选六列均不得低于随机基线，也不得低于当前 `v5.3` 对应列；相邻完全重复率和平均保留上期点位数均不得高于当前影子版。列校准候选虽把第4列从 `-0.035` 改到 `-0.013`，却把第2列降到 `-0.024`，后半段相对影子版为 `-0.087`，已撤回运行实现。
- 排序位置偏振候选使用正确顺序统计理论频率、近30/15/5期 `40%/35%/25%` 和 `n/(n+60)` 收缩，只占研究排序15%。91期平均覆盖/点亮为 `3.363 / 2.824`，相同率 `13.3%`，AUC `0.4957`，否决，不进入影子版。
- 固定测试动态分层概率排名 `95%` 与稳定短期信号排名 `5%` 的微调，不搜索权重。91期平均覆盖升到 `3.495`，最弱列由 `-0.057` 收窄到 `-0.035`，平均仅保留上期 `1.889/6` 个点位；但范围点亮降到 `2.956`，相对 `v5.3` 为 `-0.011`，相邻完全重复率升到 `1.1%`，091单期由6球降为5球。该方案仅保留为预注册研究候选等待新增开奖，不替换 `v5.3`，也不在同一91期样本上搜索其他混合权重。
- 固定测试条件似然排名 `95%` 与稳定短期信号排名 `5%` 的组合；其91期覆盖/点亮为 `3.462 / 2.945`，后半段相对 `v5.3` 为 `-0.022`，091单期覆盖由6球降为5球，已否决并撤回实现。
- 顺序条件全局优化先用30/15/5期排序位置概率直接选择六列，覆盖/点亮降到 `3.176 / 2.725`、完全重复率升到 `27.8%`；再以固定20%作为 `v5.3` 动态分层概率的位置校正，仍仅为 `3.418 / 2.890`，第4、6列均未改善。按点位预测分数做无权重的最大最小公平选择虽把覆盖升到 `3.538`、平均保留点位降到 `1.778`，却使第4、6列退化到 `-0.046 / -0.068`，完全重复率升到 `2.2%`。三项均否决并撤回运行实现，不继续搜索位置权重或公平权重。
- 54个候选的族级 Reality Check/SPA 均未通过：红球覆盖 p 值 `0.914 / 0.159`，范围点亮 p 值 `0.914 / 0.623`。正式版继续保持 `position-v5.0-annual-short`，当前影子继续保持 `v5.3`。

### v5.3 动态分层影子

- 正式版继续保持 `position-v5.0-annual-short`，不修改正式算法和既有密封记录。当前新影子为 `position-v5.3-annual-short-dynamic-hierarchical-global-shadow`；旧 `v5.2` 记录与研究实现保留为只读对照，不再自动生成新记录。
- `v5.3` 先在目标期之前最多30期数据上训练共享的正则化逻辑概率模型。2026年初不足30期时仍只从2025年末补足。模型以理论红球率 `6/33` 为截距先验，使用半衰期3/7/15期频率、遗漏、上期重号/邻号、三区负载、012路负载和奇偶负载；33个号码只保留强收缩的号码效应，避免短样本自由拟合。
- 每个红球得到下一期概率后，候选中心分数为其 `±1` 三个号码概率之和。沿用现有全局组合搜索，精确选择六个间距至少3的中心，因此始终形成六个互不重叠范围并覆盖18个号码；原 `v5.2` 范围事件分只用于总分完全相同时的固定二级判据。
- 年度研究新增候选族级检验。Reality Check 使用固定种子、共同循环区块自助法，对所有候选相对当前影子的差值同时重采样，并报告修正后的 p 值和最佳候选选择修正95%下界；SPA 风格检验使用学生化均值并剔除明显劣势候选。每个模型同时直接输出最新一期的点位、红球覆盖、范围点亮以及相对 `v5.3` 的单期差值，不再从年度均值反推新增证据。红球覆盖与范围点亮分别报告，均不能替代至少100个开奖前密封配对样本的正式晋升门槛。
- 固定测试过把六个范围点亮概率的几何平均作为全局目标，用来惩罚单列低概率。88期红球覆盖由 `3.420` 升到 `3.443`，范围点亮仍为 `2.943`，最弱列提升仍为 `-0.055`，相邻完全重复率反而由 `0%` 升到 `1.15%`。该目标没有改善每列点亮或相同化，运行实现已撤回，不继续搜索混合权重。
- 动态分层模型的原始范围概率已加入严格滚动的 Brier/AUC 审计。88期 Brier 为 `0.248972`，接近随机概率基线 `0.248736`，明显好于 `v5.2` 范围信号的 `0.262378`；同期AUC为 `0.51197`，前后半段为 `0.51527 / 0.50867`，也高于 `v5.2` 的 `0.50220 / 0.50738 / 0.49701`。但五段AUC为 `0.52328 / 0.51457 / 0.53163 / 0.52098 / 0.47111`，最后18期仍低于随机排序。当前弱列主要来自近期信号阶段失效，不能按已知结果写死惩罚点位32或继续搜索窗口和权重；保持 `v5.3` 影子等待真实前向结算。
- 预先固定的结构补全研究候选在同一分层逻辑模型上一次性加入前两期重号/邻号、上期二邻号、质合负载和近3期局部密度，不搜索子集或权重。原始范围概率 Brier 为 `0.248675`、首次略优于随机基线，AUC为 `0.52481`，五段为 `0.54905 / 0.55385 / 0.49302 / 0.52656 / 0.50114`；平均点亮由 `2.943` 升到 `2.966`，最弱列提升由 `-0.055` 收窄到 `-0.021`，相邻完全重复率仍为 `0%`。但红球覆盖降到 `3.386`，后半段点亮相对 `v5.3` 为 `-0.091`，最差五段为 `-0.222`，候选族 Reality Check p值为 `0.909`。因此仅保留固定研究候选等待新增开奖，不创建 `v5.4`、不改写089密封预测，也不在现有88期上拆特征调参。
- 按专业预测组合的简单平均原则，固定测试基础动态概率与结构补全概率的50/50组合，不搜索比例。组合平均红球覆盖/点亮为 `3.432 / 2.989`，相对 `v5.3` 为 `+0.011 / +0.045`；点亮前后半段优势为 `+0.068 / +0.023`，相邻完全重复率仍为 `0%`，平均仅保留 `1.64/6` 个上期点位。原始概率AUC为 `0.51942`，五段为 `0.53558 / 0.53999 / 0.50719 / 0.52952 / 0.48501`。但最弱列提升降为 `-0.066`，最差五段点亮优势为 `-0.118`，点亮 Reality Check p值为 `0.852`、SPA p值为 `0.725`。它改善总平均但未证明每列稳定改善，仅保留研究候选，不替换 `v5.3`，不继续搜索组合权重。
- 固定6球条件概率诊断把逻辑模型的33个边际概率转换为“每期恰好选6球”的条件泊松分布，并用六阶基本对称多项式精确计算三球范围至少命中1球的概率；没有窗口、权重或阈值参数。88期严格滚动Brier由 `0.248972` 小幅改善到 `0.248845`，同期AUC仍为 `0.51197`，但按该概率做六范围全局选择与 `v5.3` 的88期结果完全相同，六列命中、五段优势和重复率均没有变化。因此只保留概率校准诊断和精确组合公式回归，不登记冗余候选、不创建 `v5.4`，也不继续变换效用函数。
- 进一步预先固定 `annual-short-dynamic-conditional-hierarchical-range-global`：不再把一期开奖拆成33个独立二分类结果，而是在训练阶段直接最小化“33球中恰好选6球”的条件似然；特征、30期窗口、学习率和正则均沿用 `v5.3`，没有新增可搜索参数，最终直接最大化精确范围点亮概率。88期一次性滚动结果为红球覆盖/点亮 `3.455 / 2.955`，相对 `v5.3` 为 `+0.034 / +0.011`，红球前后半段优势 `+0.045 / +0.023`，五段红球优势均不为负；但只改变 `3/88` 期，红球/点亮配对95%下界仍为 `-0.004 / -0.011`，点亮后半段为0，最弱列提升仍为 `-0.055`，相邻完全重复率由0升到 `1.15%`。因此仅保留研究候选等待新增开奖，不创建 `v5.4`、不写089账本，也不根据这3个历史分歧期调整正则或效用函数。
- `2026089` 开奖为 `05,18,23,24,27,33 + 03`。正式版、`v5.2`、`v5.3` 的开奖前密封预测均覆盖4个红球并点亮3个范围，新增真实样本仍未形成版本胜负。加入089后，`v5.3` 的89期红球覆盖/点亮为 `3.427 / 2.944`，相邻完全重复率仍为0；固定6球条件似然候选为 `3.461 / 2.955`，但仍只在3期与 `v5.3` 分歧、最弱列提升同为 `-0.048`、重复率为 `1.14%`，两项95%下界仍为负。结构补全和50/50组合在089均只覆盖2个红球，后半段与最差分段门槛继续失败；候选族红球/点亮 Reality Check p值为 `0.892 / 0.888`，SPA p值为 `0.155 / 0.639`，均不显著。保持 `v5.3`，不创建 `v5.4`。
- `2026090` 已按089数据自动密封：正式版点位为 `6,11,17,24,28,32`，`v5.3` 为 `2,5,9,15,18,21`；两版蓝球均为独蓝 `6`、两码 `1,6`、三码 `1,5,6`。两组点位明显分歧，可形成新的真实前向配对样本；开奖前不再修改。
- 预先固定测试了成对排序分层模型：每个训练期直接比较6个开奖号与27个未开奖号，沿用 `v5.3` 的特征、窗口、正则及学习率，再转换为固定6球概率并做六范围全局优化，不搜索任何参数。89期红球覆盖为 `3.438`，相对 `v5.3` 仅 `+0.011`；范围点亮为 `2.933`，反而 `-0.011`。红球/点亮前半段分别退化 `-0.068 / -0.045`，最差五段为 `-0.111 / -0.056`，相邻完全重复率由0升到 `1.14%`；Brier为 `0.249202`、AUC为 `0.51167`，也弱于当前动态概率。089期它与 `v5.3` 点位相同并同为覆盖4、点亮3，没有新增前向优势。该方向一次性否决，运行实现已撤回，不测试配对权重、间隔损失或正则网格。

- 正式点位规则已切换到 `position-v5.0-annual-short`。2026年的点位预测优先使用本年截至目标期前的记录，样本不足30期时，仅从2025年末尾借足缺口，绝不继续向更早年份扩展。
- 六个点位的号码分数由近30期 `40%`、近15期 `35%`、近5期 `25%` 组成，再以三区、奇偶、012路、质合、重号和邻号的短期转移信号做最大 `8%` 的轻量修正。仍以六个不重叠的 `±1` 范围贪心覆盖18个红球。
- 年度回测和点位研究只统计最新期号所属年度。当前2026年已开奖85期的滚动结果为平均覆盖 `3.388`，随机期望 `3.273`，提升 `+0.116`；样本尚不足100期且提升95%下界为负，因此只作为当前观察值，不能视为统计上可用或稳定胜过旧规则。
- `v4.0` 及其影子版本的历史回测和既有密封账本均保留为只读档案，不改写；它们不再自动冻结新的前向记录。新规则版本会自动隔离旧缓存，年度缓存仍只保存在安装目录。
- 蓝球不在本轮目标内，继续保持原有算法；点位研究与晋升只看六个红球点位的年度命中，不以蓝球或联动红球结果决定是否升级。
- 首轮年度短期候选比较中，`position-v5.1-annual-short-no-structure-shadow`（仅移除8%结构修正）在2026年85期回测平均覆盖 `3.447`，比 `v5.0` 高 `+0.059`，前后半段优势为 `+0.095 / +0.023`；但配对95%下界为 `-0.001`，尚不能升级正式版。它只作为新一期的影子记录积累真实前向样本。
- 结构成分消融进一步拆分了三区/奇偶/012路/质合与重号/邻号。类别信号候选平均覆盖最高为 `3.412`，相对正式版 `+0.024`、配对95%下界 `-0.009`；重号/邻号候选同样未通过配对下界，且部分方案后半段优势为0。没有成分优于完全移除结构修正的 `v5.1`，因此不新增影子版本，也不按已开奖85期继续细调结构权重。
- 指数衰减候选无法同时改善命中和动态性：半衰期3/5期虽把相邻完全重复率降到 `4.8% / 9.5%`，平均覆盖却降到 `3.341 / 3.353`；半衰期30期覆盖 `3.424`，重复率反而升到 `46.4%`，全部否决。
- 当前影子改为 `position-v5.2-annual-short-range-event-global-shadow`：它按近30/15/5期 `40%/35%/25%` 直接估计每个 `±1` 范围至少命中一个红球的概率，再全局选择六个互不重叠范围。2026年85期红球平均覆盖 `3.459`、点位平均点亮 `2.918`，正式版为 `3.388 / 2.824`；两项前后半段优势均为正，重复率由 `15.5%` 降到 `11.9%`。配对95%下界仍为负，因此只进入真实前向影子验证，不升级正式版；`v5.1` 既有密封记录保持只读。
- `2026086` 已密封首个 `v5.2` 真实分歧样本：正式版点位为 `6,11,15,18,24,28`，`v5.2` 为 `6,9,13,16,24,28`。两条记录均有独立不可变哈希，开奖前不再修改。
- 截至 `2026089`，Debug 安装目录中的真实前向账本已结算 `2026086-2026089`，并密封 `2026090`。正式版与 `v5.2` 四期红球覆盖均为 `3 / 3 / 4 / 4`，平均 `3.500`；六个范围点亮每期均为3。`v5.3` 从089开始积累，首期同样为覆盖4个红球、点亮3个范围。样本仍远低于100个开奖前配对门槛。
- 以 `2026088` 为数据截止期重跑88期年度滚动研究，正式版红球覆盖/点亮均值为 `3.386 / 2.830`，`v5.2` 为 `3.455 / 2.920`；`v5.2` 两项绝对随机提升95%下界仍为 `-0.046 / -0.034`。现有红球覆盖同分候选虽相对 `v5.2` 总均值为 `+0.011 / +0.023`，但后半段红球差转为 `-0.023`，最后18期两项均为 `-0.056`，继续不具备影子资格。
- 稳定短期排名信号在88期的同期AUC为 `0.5282`，前后半段 `0.5190 / 0.5374`，五段为 `0.5378 / 0.5297 / 0.5140 / 0.5417 / 0.5175`。为检验“同分判据过弱、等权共识过强”这一缺口，预先固定测试了 `75%` 基础范围排名与 `25%` 稳定短期排名融合，不搜索比例。候选相对 `v5.2` 全年红球覆盖/点亮退化 `-0.091 / -0.034`，95%下界为 `-0.235 / -0.170`，前四个分段有三个红球退化；仅最后18期转正属于局部追势。该实验已撤回，不保留运行代码、不进入账本，也不再围绕同一88期搜索融合比例。
- 对2026年85期预先等分为五个连续17期区段后，`v5.2` 的红球覆盖优势为 `+0.059 / -0.118 / +0.118 / +0.235 / +0.059`，点位点亮优势为 `+0.118 / -0.059 / +0.059 / +0.235 / +0.118`。候选在4段改善、1段退化，说明优势不是单一末段造成，但仍未达到所有阶段非退化门槛；保持影子，不做历史门控或继续调参。
- 为降低纯范围事件模型的阶段方差，预先固定测试了范围事件占 `0%/25%/50%/75%` 的单球覆盖融合。最佳75%方案平均覆盖 `3.447`、点位点亮 `2.906`，均低于纯范围 `v5.2` 的 `3.459 / 2.918`，且前半段两项相对正式版均为 `-0.024`；其余融合比例整体退化。融合方向全部否决，当前影子保持 `v5.2`。
- 范围事件全局模型另测试了四组近30/15/5期时间权重。`55%/30%/15%` 为 `3.341 / 2.824`，`30%/45%/25%` 为 `3.471 / 2.941`，`25%/30%/45%` 为 `3.424 / 2.953`，等权为 `3.447 / 2.894`（依次为红球覆盖/点位点亮）。其中 `30%/45%/25%` 总体最高，但五段红球优势为 `+0.176 / -0.176 / 0 / +0.235 / +0.176`，第二段退化大于当前 `v5.2`，相邻完全重复率也由 `11.9%` 升至 `19.0%`；`25%/30%/45%` 前半段红球退化，其余方案总体更差。四组均不替换 `v5.2`，避免继续针对同一85期样本选参。
- 预先固定的层级收缩范围事件候选，将近15期概率以一个15期等效先验向近30期收缩，再将近5期概率以一个5期等效先验向收缩后的近15期收缩，不做强度搜索。结果红球覆盖 `3.341`、点位点亮 `2.835`，均低于 `v5.2` 的 `3.459 / 2.918`；相邻完全重复率由 `11.9%` 升至 `31.0%`。五段相对 `v5.2` 的红球差为 `-0.118 / -0.294 / 0 / 0 / -0.176`，点亮差为 `-0.059 / -0.235 / 0 / 0 / -0.118`，没有任何区段改善，已否决且不新增影子版本。
- 预先固定的前向Brier自适应窗候选，在每个目标期只按此前范围事件的概率误差从30/15/5期专家中选择一个窗口，平局优先更宽窗口，不搜索权重或阈值。85期红球覆盖 `3.494`、点位点亮 `2.941`，相对随机覆盖提升95%下界首次略为正 `+0.007`；但相对当前 `v5.2` 的红球优势仅 `+0.035`、95%下界 `-0.181`、前后半段 `+0.167 / -0.093`，点亮优势 `+0.024`、95%下界 `-0.153`、前后半段 `+0.119 / -0.070`，完全重复率也升至 `46.4%`。该候选存在明显阶段反转和黏滞，不进入前向影子，等待新增开奖后再复查，不围绕现有85期继续改造。
- 逐列诊断显示，`v5.2` 六列点亮率依次为 `45.88% / 49.41% / 55.29% / 34.12% / 51.76% / 55.29%`，第4列是主要弱项。新增的“列号+具体中心”交叉诊断进一步确认，第4列并非只由21、23拖累：点位18为 `7/17`、21为 `7/16`、14为 `3/11`、23为 `3/11`、19为 `3/10`，前五个高频中心合计仅 `23/65`。因此不能按已知85期结果写死惩罚21或23，后续研究必须使用目标期之前可计算的泛化信号。
- 预先固定的滚动三区可靠度校准候选，只用每个目标期之前的30期窗口，从最短5期训练样本开始，按低、中、高三区累计范围事件预测残差并校准当前概率；2026年初窗口不足时仍只从2025年末补齐，不读取2024。该候选把第4列点亮率从 `34.12%` 提到 `36.47%`，但红球覆盖由 `3.459` 降至 `3.412`、点亮由 `2.918` 降至 `2.847`；相对 `v5.2` 的红球/点亮优势为 `-0.047 / -0.071`，95%下界为 `-0.160 / -0.178`，前后半段均退化。该方向已一次性否决，不搜索区域边界、校准强度或窗口参数，也不进入前向影子。
- 预先固定的范围点亮状态转移候选，把每个 `±1` 范围上一期的点亮/未点亮作为状态，只统计目标期之前同状态后的结果，并用5期等效先验向 `v5.2` 基础概率收缩。它把相邻完全重复率从 `11.9%` 降至 `2.4%`，但红球覆盖仅 `3.259`、点位点亮仅 `2.741`，两项都低于随机期望；相对 `v5.2` 分别退化 `-0.200 / -0.176`，95%下界为 `-0.458 / -0.384`，前后半段及五段多数区间均退化。该结果再次证明不能为了减少重复强制换点，状态转移方向已否决且不搜索先验强度。
- 预先固定的短期结构近邻候选，用最新一期的三区比、012路、奇偶、质合和红球间距图形，与目标期前30期中的历史前态计算连续相似权重，再以相似前态的下一期开奖校准范围概率；有效类比样本用5期等效先验向 `v5.2` 收缩。第4列点亮率由 `34.12%` 提至 `42.35%`，但总体红球覆盖/点亮降为 `3.412 / 2.871`，相对 `v5.2` 均为 `-0.047`，95%下界为 `-0.238 / -0.186`；前半段退化 `-0.143 / -0.095`，第二个17期区段明显失效，完全重复率还升至 `32.1%`。该候选只改善局部列、没有改善总体预测，已否决且不搜索距离或融合参数。
- 预先固定的一标准误差双目标候选，先求点亮概率总和最高的组合，再把当前30期逐期点亮波动推导出的一个标准误差内组合视为同档，只在同档中最大化18码红球覆盖概率，不使用手工融合权重。该规则在 `78/85` 期改动 `v5.2`，红球覆盖/点亮降为 `3.318 / 2.800`，相对 `v5.2` 退化 `-0.141 / -0.118`，95%下界为 `-0.324 / -0.280`，前后半段都退化。统计同档范围对本任务仍过宽，已否决；后续保持范围事件主分绝对优先，红球覆盖最多只作为精确同分的研究判据。
- 年度研究新增范围事件原始分数审计，对2026年85期、每期31个候选中心共2635个严格前向事件，分别报告近30、近15、近5和 `40%/35%/25%` 加权分数的Brier误差、同期开奖排序AUC、校准偏差及五段结果。当前加权分数Brier为 `0.261692`，差于随机概率基线 `0.248659`，Brier skill为 `-5.24%`；总体AUC仅 `0.5053`，前后半段为 `0.5077 / 0.5030`。近5期AUC最高也只有 `0.5116`，同时Brier skill为 `-18.53%`，且第一、第五段AUC为 `0.4931 / 0.4905`。这说明当前最主要限制是短窗概率对下一期的排序信号极弱，而不是全局组合器缺少复杂度；不得仅凭85期中略高的AUC继续放大近5期权重。
- 动态信号审计进一步逐期检查近5相对近30动量、近15动量、加速度、遗漏、上一期点亮和连续状态。`近5-近30` 动量AUC为 `0.5216`，前后半段 `0.5090 / 0.5339`，五段为 `0.4841 / 0.5463 / 0.5312 / 0.5253 / 0.5210`；上一期点亮信号AUC为 `0.5223`，五段中四段高于0.5。高遗漏信号AUC仅 `0.4827`，五段中四段低于0.5，不支持追逐长期未亮范围；近15动量后半段降至 `0.4919`，加速度也有两段低于0.5。下一步仅允许把 `近5-近30` 动量作为主范围事件分完全相同时的二级研究判据，不再用状态转移或大权重融合强制换点。
- 按《剑走偏锋方法对点位推荐的可用性分析》预先定义并接入三个隔离信号审计，所有信号只读取目标期以前的数据，不参与正式版或 `v5.2` 影子版选点：`jpf-midpoint` 统计上期六红两两形成的整数中点落入候选 `±1` 范围的次数；`jpf-compound-transition` 汇总上期点亮、隔1至3期点亮、上期范围外侧邻号三项证据；`jpf-tail-state` 统计上期命中候选范围尾数组的红球数，并为同尾对增加一项支持。
- 2026年85期、每期31个中心的严格前向审计中，`jpf-midpoint` 总体AUC为 `0.4768`，前后半段为 `0.4648 / 0.4885`，五段全部低于0.5；`jpf-tail-state` 总体AUC为 `0.4968`，前后半段为 `0.5087 / 0.4853`，五段仅两段略高于0.5。两者已否决，不进入点位算法，也不继续搜索参数。
- `jpf-compound-transition` 总体AUC为 `0.5214`，前后半段为 `0.5261 / 0.5167`，五段为 `0.5364 / 0.5212 / 0.5070 / 0.5522 / 0.4900`。它有正向迹象，但仍略低于更简单的 `recent-lit` 信号 `0.5223`，末段也低于0.5，尚未证明组合证据提供独立增益；因此仅允许做一次主范围事件总分完全相同时的固定二级判据研究，不直接获得影子版本或前向账本资格。
- 精确同分候选 `annual-short-range-event-global-jpf-compound-tiebreak` 在85期中只改变 `7` 期，红球平均覆盖与点位平均点亮均比 `v5.2` 高 `+0.0118`，但两项配对95%下界均为 `-0.0400`，前后半段优势为 `0 / +0.0233`，五段优势为 `-0.0588 / +0.0588 / +0.1176 / -0.0588 / 0`。候选在两个阶段退化且前半段没有改善，已否决，不创建新影子版本；`jpf-compound-transition` 只保留为新增开奖后的固定信号审计。正式版、`v5.2` 和 `2026086` 已密封预测均不改写。
- 中点正向支持五段全部低于0.5后，预先固定其反向值为 `jpf-midpoint-avoidance`，不搜索窗口、阈值或权重。该回避信号总体AUC为 `0.5232`，前后半段为 `0.5352 / 0.5115`，五段为 `0.5798 / 0.5051 / 0.5064 / 0.5181 / 0.5067`，方向一致；但这是从同一85期样本发现的反向解释，只能视为研究线索，不能当作独立验证证据。
- 精确同分候选 `annual-short-range-event-global-jpf-midpoint-avoidance-tiebreak` 只改变 `3/85` 期，红球覆盖、点位点亮、前后半段和五段相对 `v5.2` 全部为0。它消除了部分默认小号顺序，但没有提高命中，故不进入影子或前向账本；反向信号保留固定审计，等待后续未观察开奖检验，不扩大为加权融合。
- 为避免动量与中点回避量纲不同，新增无参数信号 `stable-momentum-midpoint-rank`：每期分别把31个中心的 `近5-近30` 动量和中点回避转换为同期百分位排名，再等权平均，不搜索权重。该融合信号总体AUC为 `0.5309`，前后半段为 `0.5223 / 0.5393`，五段为 `0.5378 / 0.5331 / 0.5217 / 0.5379 / 0.5239`，是当前单点排序最稳定的研究信号；但其组成和方向均来自同一85期研究，只能等待未观察开奖复核。
- 精确同分候选 `annual-short-range-event-global-stable-signal-rank-tiebreak` 改变 `5/85` 期，点位点亮相对 `v5.2` 持平，红球覆盖反而 `-0.0118`、95%下界 `-0.0519`，后半段为 `-0.0233`；五段红球差为 `0 / 0 / +0.0588 / -0.1176 / 0`。单点AUC没有转化为受互不重叠约束的六点组合提升，该候选已否决，不扩大近似同分范围或继续调权；融合信号仅保留固定审计。
- 为确认融合信号是否只是被精确同分限制，固定测试了 `annual-short-stable-signal-rank-global` 消融：直接最大化六点融合排名总和，以原范围事件分仅作同分判据，不搜索混合比例。候选改变全部85期，红球覆盖/点位点亮为 `3.447 / 2.918`，相对 `v5.2` 为 `-0.0118 / 0`；红球配对95%下界 `-0.3133`，前后半段差为 `+0.1905 / -0.2093`，五段红球差为 `-0.2941 / +0.6471 / +0.2941 / -0.4706 / -0.2353`。强烈阶段反转证明稳定的同期单点AUC仍不足以独立承担六点组合目标，该消融已否决，不再测试主目标替换或混合权重。
- 为尝试自动避开上述阶段反转，新增无参数候选 `annual-short-prequential-signal-champion-global`：每期只用此前最多30期，逐个历史子目标比较原范围事件分与稳定融合信号的累计同期AUC，累计AUC较高者负责当前期排序，相同则保留 `v5.2`；不学习权重、不选择回看窗口。候选在 `64/85` 期改变 `v5.2`，红球覆盖/点位点亮为 `3.435 / 2.847`，相对退化 `-0.0235 / -0.0706`，配对95%下界为 `-0.2776 / -0.2584`；前后半段红球差 `+0.1905 / -0.2326`，最差五段红球/点亮差为 `-0.4118 / -0.2941`。累计专家选择仍滞后于阶段变化，已否决；不继续增加近期专家表现窗口或切换阈值。
- 固定测试 `annual-short-base-stable-rank-consensus-global`：把 `v5.2` 范围分与稳定融合信号分别转换为31点同期排名后等权平均，再全局选择六个互不重叠范围，不搜索权重。候选在 `76/85` 期改变 `v5.2`，红球覆盖/点位点亮为 `3.471 / 2.953`，相对均值虽为 `+0.0118 / +0.0353`，但配对95%下界为 `-0.2190 / -0.1457`，后半段为 `-0.2093 / -0.0465`，第四段更退化 `-0.5294 / -0.4118`。该共识仍放大阶段方差，已否决；稳定融合信号的主导、动态切换、精确同分和等权共识四条组合路径全部停止。
- 新增固定动态概率候选 `annual-short-discounted-beta-range-global`：以随机6/33开奖下三号码范围的理论点亮率作为1个等效样本先验，每期将旧 `alpha/beta` 同乘 `14/15` 后加入本期点亮结果，使有效历史量趋近15期；不再组合30/15/5窗口，也不搜索折扣。信号AUC仅 `0.4992`，前后半段 `0.5052 / 0.4934`；候选红球覆盖/点位点亮为 `3.329 / 2.835`，相对 `v5.2` 退化 `-0.1294 / -0.0824`，配对95%下界 `-0.2894 / -0.2103`，五段仅第四段改善。该动态贝叶斯方向已否决，不测试其他有效样本量或先验强度。
- 按上述预先限定实现的动量精确同分候选，主范围事件总分拥有绝对优先级，仅在完全同分时最大化六点的 `近5-近30` 动量总和。它只改变 `4/85` 期，红球覆盖与 `v5.2` 完全持平为 `3.459`，点亮由 `2.918` 小幅升至 `2.929`，相邻完全重复率由 `11.9%` 降至 `10.7%`；点亮相对优势仅 `+0.0118`、95%下界 `-0.0113`，前后半段 `0 / +0.0233`。五段红球差为 `0 / 0 / +0.0588 / -0.0588 / 0`，存在对消且历史样本不足100期，因此保留为研究对照，不进入前向影子，不改写已密封预测。
- 年度研究报告现同时输出正式基线与当前影子模型标识，并为每个候选计算相对 `v5.2` 的红球、点亮配对均值、95%下界及前后半段差值。后续候选不能再只凭相对正式版或总平均晋升。
- 历史筛选门槛已固化为研究报告中的机器判定：年度历史样本至少100期；相对实际覆盖宽度随机期望的提升95%下界、前半段和后半段均大于0；相对当前影子的红球覆盖与点位点亮差值也分别要求95%下界、前半段和后半段均大于0。通过后仅代表可以新建影子并进入开奖前密封的真实前向验证，不能直接替换正式版。以当前85期复跑，没有任何候选获得前向资格；Brier自适应窗仅通过绝对覆盖门槛，失败原因为历史样本不足、红球配对未稳定超过 `v5.2`、点亮配对未稳定超过 `v5.2`。
- 正式晋升只读取前向账本：候选必须另行积累至少100个已开奖、开奖前密封且与正式版同期的配对样本，并再次通过绝对随机覆盖、红球覆盖差和点位点亮差的95%下界及前后半段门槛。历史回测记录不能计入这100个前向样本。前向样本数由共享策略常量统一驱动账本判定、结果模型和研究报告，避免门槛数值漂移。
- 五段报告也已改为直接输出相对当前影子的红球和点亮差值，并汇总最差区段。Brier自适应窗相对 `v5.2` 的五段红球差为 `+0.059 / +0.412 / -0.118 / -0.176 / 0`，点亮差为 `+0.059 / +0.235 / 0 / -0.176 / 0`，两项最差区段均为 `-0.176`。验证器会修改后续模拟开奖号并要求所有模型的早期区段报告逐字段不变，确保自适应选择和阶段统计没有读取未来数据。
- 针对全局组合主分完全相同时默认保留较小号码组合的顺序偏置，新增了只以18码预期红球覆盖作为二级判据的研究候选；主范围事件分仍拥有绝对优先级，未调任何权重。该判据改变 `11/85` 期，红球覆盖 `3.471`、点位点亮 `2.941`，相对 `v5.2` 仅 `+0.012 / +0.024`，95%下界为 `-0.050 / -0.033`，后半段红球优势为0，最差区段两项均为 `-0.059`，重复率也由 `11.9%` 升至 `13.1%`。它消除了任意同分偏置，但未证明提升预测能力，保留为研究对照且不进入前向验证；现有 `v5.2` 行为和已密封预测不变。
- 点位绝对门槛已补齐随机点亮基线。随机6/33开奖下，一个三号码范围至少命中一个红球的概率按组合补集精确计算，六个点位的随机期望点亮数为 `2.783`；期望可逐点相加，不要求六个事件相互独立。`v5.2` 的85期平均点亮为 `2.918`、提升 `+0.134`，前后半段提升均为正，但95%下界仍为 `-0.043`；Brier自适应窗和同分二级判据候选的点亮提升95%下界也分别为 `-0.017 / -0.014`。因此当前没有任何模型证明点亮数稳定超过随机。
- 历史筛选和真实前向晋升现在都要求两项绝对门槛同时通过：18码红球覆盖相对随机基线稳定为正，六个点位点亮相对随机基线也稳定为正；两项均检查95%下界、前半段和后半段。相对当前影子的红球与点亮配对门槛继续保留，避免用覆盖提升掩盖点亮退化，或反过来。
- 确定性前向验证覆盖了两道绝对门槛的独立性：100期内每个点位各命中一个红球时，18码覆盖与六点点亮均通过；100期内六个红球全部落入18码池、但集中在两个点位范围时，红球覆盖门槛通过而点亮门槛单独失败。前向比较报告也分别输出两项绝对门槛，最终点位晋升仍要求二者同时通过。

## 当前持续目标（2026-07-27）

- 运行输出只保留六个点位和严格嵌套的三档蓝球；现阶段研究只提升六个点位的 `±1` 红球覆盖命中率，蓝球不参与点位候选晋升。
- 旧 `position-v4.0` 的晋升证据只保留为历史档案；新版本从年度短期口径重新积累证据，已有账本记录和已经密封的 `2026086` 旧版本预测不改写。
- 点位候选必须使用目标期之前的数据滚动预测，并只检查最新年度的实际覆盖宽度随机基线、前后半段及相对当前年度短期基线的配对95%下界。
- 点位专用研究命令不计算蓝球和联动红球、不启动窗口、不写前向账本：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-point-research 400 8
```

截至 `2026085`，当前 `80%` 全历史频率加 `20%` 近100期频率基线在3200期平均命中 `3.31875`，随机覆盖期望 `3.27273`，提升95%下界 `+0.00657`；八段平均命中依次为 `3.3625 / 3.3300 / 3.2975 / 3.3750 / 3.3250 / 3.2600 / 3.2575 / 3.3425`，其中两段低于随机期望，尚未达到跨阶段稳定门槛。

本轮新增并否决指数衰减400/800/1600期、滚动400/800/1600期、总体重号/邻号/遗漏状态和号码级正则化转移概率。最好新增模型 `stable-ball-transition-20` 的3200期平均命中为 `3.32063`，仅比基线高 `0.00188`，相对基线配对95%下界为 `-0.01590`，且分段仍有退化，因此不进入影子版本。

后续点位专用实验继续否决了经验贝叶斯短窗收缩、样本量渐进近100期权重及过去400/800期在线专家选择，三类方案均低于当前基线。近30期三区、奇偶和012路回归模型平均命中 `3.325`，八个400期窗口均高于随机覆盖期望，但最新与最早两个预留验证窗口相对基线都为 `-0.0425`，属于开发阶段过拟合。结构转移、等权共识及保守最小值模型的相对基线95%下界也均为负，不进入影子版本。研究报告固定把中间六段作为开发样本、首尾两段作为验证样本单独输出。

以 `position-v4.0` 为新基线后，进一步测试了46维在线逻辑回归，特征包括多尺度频率、遗漏、重号、邻号、三区、奇偶、012路和号码级转移。原始模型3200期平均命中 `3.29531`；按80%正式基线加20%在线模型锚定后为 `3.31750`，仍比正式基线少 `0.00125`，配对95%下界为负，均已否决。验证器额外检查修改后续窗口开奖号不会改变较早窗口的任何研究模型结果。

联网调研后的评估规范继续采用两类公开方法：时序交叉验证必须按时间顺序训练和验证，不能用未来样本训练过去目标（[scikit-learn TimeSeriesSplit](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.TimeSeriesSplit.html)）；在线候选必须逐期先预测、开奖后再学习，模拟真实生产顺序（[River progressive validation](https://riverml.xyz/latest/api/evaluate/progressive-val-score/)）。研究器现有滚动路径符合这两项原则，真实前向账本仍是最终晋升依据。

新增研究模型 `drift-adaptive-shrunk-recent20`，参考 [ADWIN 自适应窗口](https://riverml.xyz/latest/api/drift/ADWIN/) 的分布漂移思想：每期只用此前数据，以32自由度、1%显著性的卡方同质性检验在1600/800/400/200/100期中选择最长稳定后缀，再把局部频率按经验贝叶斯可靠度向长期频率收缩，且局部影响最多占20%。3200期平均命中 `3.29438`，相对正式版 `-0.02438`，配对95%下界 `-0.06201`；点位点亮数相对正式版 `-0.01844`，开发/验证优势为 `-0.01792 / -0.04375`。该方向开发与验证同时退化，已否决，不进入影子版，也不继续调检验阈值。

重复度审计确认了正式版过于稳定：最近100个预测中69对相邻期完全相同，平均保留 `5.65/6` 个点位，最长连续35期完全相同；3200期研究样本的相邻期完全重复率为 `70.02%`。研究报告因此新增相邻期完全重复率、平均保留点位数、与正式版分歧期数，以及仅在分歧期计算的18码池和点位点亮数优势及95%下界。后续动态候选不仅要降低无意义重复，还必须在真正改变点位的期次证明命中提升。

`forecast-first-multiscale-structure` 先以长期20%、近30期30%、近15期30%、近5期20%形成动态频率，再按上一期三区、奇偶、012路状态及重号、邻号、遗漏条件概率修正。它把相邻期完全重复率降至 `10.72%`、平均保留 `4.05/6` 个点位，但3200期平均命中仅 `3.27969`，相对正式版 `-0.03906`；中间开发段 `-0.06208`、首尾验证段 `+0.03000`，阶段方向冲突，已否决。

`confirmed-trend-swap` 从正式点位出发，仅当替代区间在近30/15/5期方向一致，且近30期配对优势单侧90%下界大于0时才替换。重复率降至 `49.58%`、平均保留 `5.30/6` 个点位，但整体优势仍为 `-0.01313`；1285个分歧期平均优势 `-0.03268`、95%下界 `-0.08205`，开发/验证均退化，已否决。该结果说明近期热度的一致性本身不是有效的下一期预测证据，不能用强制换号制造动态感。

`position-v4.0` 点位单独晋升证据：3200期内旧正式 `position-v3.2.2` 平均覆盖 `3.01594`，新正式平均覆盖 `3.31875`；同期配对提升 `+0.30281`，95%下界 `+0.25488`，前后半段提升 `+0.27375 / +0.33188`。3200期独蓝、两蓝、三蓝预测差异数均为0，证明本次没有改动蓝球算法。复查命令：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-backtest-point-promotion 3200 0
```

## 当前版本

- 规则版本：`position-v5.0-annual-short`（实际运行标识还包含固定配置哈希）。
- 年度数据：点位预测结果从 `2026001` 开始生成；导出默认只包含最新期号所属年度的已开奖期次和该年度下一期，缓存按 `position_export_cache.<年份>.json` 分开保存在安装目录；进入新年度后自动生成新的年度缓存，旧年度缓存互不覆盖。
- 点位：仅使用本年度最近30期；本年度不足30期时，以紧邻上一年度的末尾期次补足。近30/15/5期权重为 `40%/35%/25%`，再用短期三区、奇偶、012路、质合、重号和邻号信号作轻量修正，并逐点最大化未覆盖的 `±1` 区间概率。
- 输出维度：六个点位展开为最多18个实际红球；蓝球输出为独蓝、两码围蓝和三码围蓝，不再派生其他红球组。
- 蓝球：完整保留 `position-v3.2.2` 正式算法；公式法和排除法只作为内部评分依据，按综合分排序产生严格嵌套的独蓝、两码围蓝和三码围蓝。
- 可用门槛：至少100期滚动样本；红球点位相对实际覆盖宽度随机期望的提升，其95%下界和前后半段提升都必须大于0；独蓝 Wilson 95% 下界和前后半段命中率都必须高于随机基线 `6.25%`。前向晋升还要求所有发生变化的点位和蓝球维度分别通过配对非退化门槛；独蓝另需至少30个有效分歧且候选胜率 Wilson 95% 下界大于50%。
- 当前目标下的点位单独前向门槛：至少100个同期开奖配对样本；候选自身18码池相对实际覆盖宽度随机期望的95%下界及前后半段提升均大于0；候选相对正式点位的18码池命中差和六个 `±1` 点位范围被点亮数量差，其配对95%下界及前后半段差值都必须大于0。蓝球完全相同时不要求制造蓝球分歧，通用多维门槛继续保留供其他升级使用。

## 影子候选

- 当前点位候选为 `position-v4.5-shadow`。它先在 `position-v4.0` 基线上加入按目标开奖日期前后 `±14` 天统计的往年同期频率，并使用240期等效先验向长期概率收缩；收缩结果只占15%。随后与 `position-v4.3-shadow` 的120期先验、5%三区/012路转移修正等权融合，再用原贪心覆盖选择六个互不重叠的 `±1` 点位。
- 3200期生产路径配对中，`position-v4.5-shadow` 平均命中 `3.32844`，正式版为 `3.31875`；点位在808期发生变化，平均优势 `+0.00969`，95%下界 `-0.00437`，前后半段优势 `+0.01688 / +0.00250`。开发/验证优势为 `+0.00833 / +0.01375`，最近400期优势为 `+0.030`，但八段中仍有三个阶段小幅退化，因此只进入前向影子验证，不晋升正式版。
- `position-v4.5-shadow` 的独蓝、两蓝、三蓝与正式版在3200期差异均为0。`2026086` 已密封前向预测，正式版点位为 `2,6,9,14,18,23`，v4.5点位为 `2,8,14,18,23,31`。
- 研究器另加入 `online-v45-gated-400`：每个目标期只用此前400期的 v4.5 与正式版点位命中差决定是否启用 v4.5。3200期平均优势仍为 `+0.00969`，点位配对95%下界由 v4.5 原版的 `-0.00437` 收敛到 `-0.00152`，18码池下界为 `-0.00266`；仍未通过正式晋升门槛，且不写入前向账本。
- v4.5 与正式版按25%/50%/75%做静态混合的三组实验整体优势为 `-0.00062 / -0.00062 / +0.00125`，点位95%下界均为负，证明简单降低近期信号权重不能解决阶段漂移，三组均不进入影子版本。
- 新增的 `online-brier-hedge` 用每个红球的 Brier 概率损失更新标准专家权重，避免只按整期命中数更新；3200期平均优势为 `-0.00688`，点位95%下界 `-0.01995`，18码池优势 `-0.00656`，开发/验证优势 `-0.00625 / -0.00875`，已否决且不进入前向账本。
- 对 v4.3 固定测试了200/400/800期历史门控、400期双指标滚动门控及两个200期子段门控。最佳400期门控平均优势 `+0.00312`，八段最差优势为0，18码池95%下界 `+0.00001`，但点位配对95%下界仍为 `-0.00023`；严格门槛要求两项下界都大于0，因此不新建版本，继续用已密封的 v4.3 前向记录观察。
- 针对往年同期窗口另测试了闰年无关的月日序号，把所有日期映射到同一闰年后再计算 `±14` 天距离。该修正使3200期平均优势由 v4.5 的 `+0.00969` 降为 `+0.00188`，点位95%下界降为 `-0.01233`，已否决；原 v4.5 算法和已密封预测不改写。
- `position-v4.3-shadow` 保留为低方差对照。它保留 `position-v4.0` 的长期/近100期概率和贪心覆盖，只使用上一期三区与012路计数状态，采用120期等效先验及5%修正强度；奇偶转移不参与。
- `position-v4.3-shadow` 完整沿用 `position-v4.0` 的独蓝、两蓝、三蓝算法，前向晋升只评估点位。
- 3200期研究中，`position-v4.3-shadow` 对应模型平均命中 `3.32219`，正式版为 `3.31875`；18码池配对优势 `+0.00344`、95%下界 `-0.00018`，中间六段开发样本优势 `+0.00333`，首尾验证样本优势 `+0.00375`。
- 六个 `±1` 点位范围被点亮数量同期提升 `+0.00312`，95%下界为 `0`，开发与验证优势分别为 `+0.00333 / +0.00250`。严格门槛要求下界大于0，因此仍不能晋升正式版。
- 最新400期生产实现平均命中 `3.3725`，正式版为 `3.3625`；前后半段优势 `+0.015 / +0.005`，配对95%下界 `-0.00384`，12期点位发生变化，独蓝、两蓝、三蓝差异均为0。
- 先验强度60/120/240期都保持开发与验证双指标为正；120期先验下5%与10%修正均稳定，15%开始增大方差。`position-v4.3-shadow` 选择历史配对下界更高的5%方案。
- 成分消融显示：120期先验、5%强度下，三区单独模型和012路单独模型均不如等权组合；三区:012路为2:1或1:2也降低整体或验证优势，因此 `position-v4.3-shadow` 保持等权，不再按已观察历史微调。
- `position-v4.2-shadow` 继续自动冻结，作为30期先验、10%三区/012路转移对照；`position-v4.1-shadow` 继续作为20%三区/奇偶/012路全量转移对照。已有密封预测均不改写。
- 邻近频率网格（长期权重60%-100%、近30/50/100期）均未稳定优于 `80%长期 + 20%近100期` 基线；全局动态规划六段覆盖平均命中 `3.31125`，低于贪心基线 `3.31875`，两类实验均不进入运行版本。
- 星期条件频率在5%-30%权重下全部未通过配对下界；往年同期日期模型仅 `±14` 天附近出现局部正值，邻近7/10/18/21/30/60天半径方向不稳定；直接最大化点位范围事件概率会使首尾验证样本的18码池平均退化 `-0.08125`。三类方案均已否决。
- 三区×012路九格联合状态在5%/10%/15%强度下整体退化；逐点位三号码范围的重号/邻号局部转移同样在开发和验证样本退化。两类更细状态均已否决，避免以稀疏条件替换稳定的分组收缩。
- `position-v4.1-shadow` 在 `position-v4.0` 的长期/近100期概率基础上，按上一期三区、奇偶和012路计数状态，使用目标期之前的历史转移分布做20%正则化修正；仍用贪心方式选择六个点位并覆盖18个不同红球。
- 截至 `2026085`，3200期平均命中 `3.32000`，正式版为 `3.31875`，配对优势仅 `+0.00125` 且95%下界为负，因此历史证据不足以晋升。最新400期生产回测平均命中 `3.390`，正式版为 `3.3625`。
- 固定的首尾两个400期验证样本中，候选相对正式版平均提升 `+0.01375`、95%下界 `+0.00153`；中间六段开发样本平均退化 `-0.00292`。阶段方向冲突，因此只冻结真实未来预测，不再利用已观察历史调参。
- `position-v3.9-shadow` 的点位逻辑已经进入 `position-v4.0`，其蓝球路径保留为归档实验，不再自动冻结。
- 下列全历史和分段配对结论来自上一版 `position-v3.5-shadow`，仅作为研发记录；`position-v3.6-shadow` 仍需重新积累真实前向样本。
- 覆盖3380期的全历史滚动审计中，红球点位均中 `3.325`、随机基线 `3.273`、提升95%下界 `+0.0137`，前后半段提升均为正，红球长期门槛通过；独蓝命中 `6.83%`、Wilson下界 `6.03%`，蓝球长期门槛未通过，因此整体仍不可晋升。
- 主版与候选按同期开奖做八个互不重叠400期历史配对时，候选红球在八段中全部通过优势门槛；每期命中差的95%下界介于 `+0.083` 和 `+0.245`，每段前后半段差值均为正。独蓝仅最新400期通过配对优势门槛，其他七段未通过，整体仍不可晋升。
- 3380期整体配对中，候选红球每期比主版多中 `0.314`，差值95%下界 `+0.267`，前后半段优势为 `+0.291 / +0.337`；独蓝有效分歧胜负为 `206:177`，候选胜率Wilson下界 `48.78%`，未证明优于主版。
- 更早的八个互不重叠400期阶段命中率在 `5.50%` 到 `9.50%` 之间，全历史滚动命中率为 `6.98%`，说明仍有明显阶段漂移，必须等待真实前向样本。
- 已归档候选的兼容修正版 `position-v3.4.1-shadow` 保留近30期蓝球公式评分，红球仍使用主版点位逻辑；可用 `--position-backtest-rolling-hot` 单独复查。
- 已归档候选的兼容修正版 `position-v3.3.1-shadow` 使用五个历史公式可靠度加权投票，不再自动冻结。

影子版本回测命令：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-backtest-shadow 200 0
```

第二个数字参数为距最新期开奖的偏移期数。

归档公式集成版本回测命令：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-backtest-ensemble 200
```

归档近30期蓝球版本回测命令：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-backtest-rolling-hot 200
```

## 已观察回测

以下窗口已经用于开发审计，后续不得再作为独立留出集宣称有效：

| 窗口 | 点位均中/随机 | 点位提升95%下界 | 独蓝 | 两码 | 三码 | 独蓝95%下界 | 独蓝前/后半段 | 结论 |
|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 100期 | 3.090 / 3.109 | -0.239 | 3.00% | 11.00% | 19.00% | 1.03% | 4.00% / 2.00% | 红蓝均不可用 |
| 200期 | 3.075 / 3.081 | -0.161 | 4.50% | 10.50% | 17.00% | 2.39% | 6.00% / 3.00% | 红蓝均不可用 |
| 400期 | 2.973 / 3.028 | -0.162 | 4.50% | 10.25% | 17.50% | 2.87% | 4.50% / 4.50% | 红蓝均不可用 |

独蓝置信下界仍低于随机基线；点位命中也未稳定超过按实际覆盖宽度计算的随机期望。因此当前版本没有统计上可用的预测优势。

## 红球候选实验

- 已测试按点位 `±1` 实际覆盖区间直接最大化多尺度热度，不再优先给点位中心号码本身打分。
- 该方案在最近400期相对自身随机覆盖基线提升 `+0.117`，但更早八个互不重叠400期阶段仅五段为正，最差阶段为 `-0.120`，不具备跨阶段稳定性。
- 近15期、近30期、近100期单窗口也未稳定通过全部阶段，不进入候选。
- 预先固定的长期频率模型中，`80%` 全历史加 `20%` 近100期在八个400期阶段有六段为正，整体平均提升 `+0.045`、95%下界 `+0.006`、最差阶段 `-0.015`。
- 该模型在未参与筛选的早期181期留出集上平均命中 `3.453`，随机基线 `3.273`，提升 `+0.180`、95%下界 `+0.013`，前后半段均为正，因此曾进入 `position-v3.5-shadow`；真实有效性仍只由后续前向样本决定。
- 同一概率模型改用动态规划求六个不重叠区间的全局最优解后，八段整体提升95%下界降至 `-0.001`，早期留出下界降至 `-0.035`；该改动放大了估计噪声，已否决，继续使用经留出验证的贪心覆盖。

## 蓝球候选实验

- 长期频率与近100/30期频率的固定混合均未通过；其中最好方案八段合计命中 `6.69%`，Wilson 95%下界仅 `5.87%`。
- 在近5/8/16/30/50/100期热号模型间按过去200期前后半段表现动态选窗，八段合计命中 `6.94%`、95%下界 `6.11%`，早期留出仅 `4.97%`，已否决。
- 当前 `position-v3.6-shadow` 将近30期热度作为公式评分的一部分，并与排除评分合并，等待真实前向样本，不再针对已观察窗口继续选参。
- 在线热度、遗漏、精确转移和类别转移专家集成在3200期独蓝仅命中 `6.69%`，且单段最低 `3.75%`，已否决。
- 保留当前独蓝、仅用概率模型补第二和第三蓝时，3200期两蓝由 `13.03%` 提升到 `14.19%`、三蓝由 `18.91%` 提升到 `19.81%`；但八段中两蓝和三蓝均存在负向窗口，累计及滚动置信门控仍未消除回撤，因此未进入影子版。
- 正式公式排名与影子排除排名交叉补位、强制012路/大小/奇偶多样化、按开奖星期建模均未在八段保持优势；其中开奖星期模型使3200期独蓝从 `7.09%` 降至 `6.91%/6.75%`，全部否决。

## 验证命令

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-backtest 200
```

主版与当前候选的同期开奖配对回测；第三个参数为距最新期开奖的偏移期数：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-backtest-compare 400 0
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-backtest-compare 400 400
```

该命令不启动窗口、不写回测数据文件，只输出 JSON。

项目内不依赖外部测试框架的确定性验证器：

```powershell
dotnet run --project Verification\SsqAnalyzer.PositionVerification.csproj -c Release
```

验证器覆盖点位边界、相同快照确定性、修改目标期之后数据不影响预测、蓝球综合排名与严格嵌套、账本幂等结算、规则版本隔离和损坏账本拒绝覆盖。验证数据只写系统临时目录并在退出前删除。

查看当前规则版本的真实前向样本状态：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-forward-status
```

不启动窗口，生成并冻结紧邻下一期预测：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-freeze-next
```

该命令只有在目标期未开奖且早于预计开奖截止时才写账本；输出中的 `Frozen` 明确表示本次预测是否已进入前向样本。

冻结当前影子候选（不改变主界面版本）：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-freeze-shadow
```

正常启动程序时，以及每次开奖数据更新后，实验协调器会自动按“先结算旧期，再冻结下一期”的顺序维护主版本及 `v4.1/v4.2/v4.3` 三个点位影子版本。也可显式执行：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-freeze-all
```

自动协调器不会在 `--position-forward-status`、`--position-backtest` 或 `--position-backtest-shadow` 中创建，三种命令保持只读。

CLI 场景需要在数据文件更新后显式结算时执行：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-reconcile
```

`--position-forward-status` 只读取已经结算的状态，不再隐式写账本。

每条前向记录的期号、数据快照、规则版本、运行ID、冻结时间和全部预测号码会计算 SHA-256 `PredictionHash`；开奖结果和结算指标不参与该校验，因此只能追加结算，不会改变原预测封存。封存已有未校验记录：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-seal-ledger
```

加载时只要校验值与不可变预测不一致，账本会被判定损坏并拒绝覆盖。该校验用于发现意外或普通手工修改，不等同于带私钥的数字签名。

只读审计正式账本或指定副本：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-audit-ledger
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-audit-ledger C:\tmp\ledger-copy.json
```

`--position-forward-status` 的 `AllSummaries` 会按规则版本分别输出主版本和影子版本，禁止把不同版本记录混在一起统计。

主版本与影子版本必须按同期开奖做配对比较：

```powershell
dotnet bin\Release\net10.0-windows\SsqAnalyzer.dll --position-compare-versions
```

审计指定副本时可依次传入主版本ID、候选版本ID和账本路径；正式统计默认使用安装目录账本。

候选晋升必须同时满足：候选自身通过红蓝绝对可用门槛；至少100个已结算配对样本；若红球预测有变化，候选点位相对主版本优势的95%下界大于0；独蓝至少30个仅一方命中的有效分歧，且候选在分歧中的胜率Wilson 95%下界高于50%。未同时满足时不得替换主版本。

前向账本保存在程序安装目录的 `position_forward_validation.json`。只冻结紧邻下一期、未开奖、`live` 模式且早于预计开奖时刻（北京时间开奖日 21:15）的结果；同一期同一规则版本只保留首次冻结值。数据更新后只补写实际开奖与评估字段，原预测字段不修改。

## 冻结规则

### 2026-09-07 数据更新与滚动复核

- 安装目录数据已前向更新至 `2026103`。正式版与当前影子版在该期的开奖前预测哈希保持不变，并已完成结算；随后只冻结了 `2026104`，没有用已开奖数据回写预测。
- 修复了 `--position-update-and-freeze` 的 WPF 同步上下文死锁：网络更新改为在线程池中等待，仍复用同一 `DataService`、账本结算和实验协调器链路。主项目、验证项目均无警告错误，验证器通过 `21/21`。
- 截至 `2026103` 的最新年度滚动研究仍没有可晋升候选。动态稳定融合模型的平均点位命中约 `3.456`、平均范围点亮约 `2.951`，但相对当前影子的配对95%下界仍为负，且列级非退化门槛未通过；它只保留为研究对照，不进入前向账本。
- 当前影子在 `2026103` 的六点位范围实际点亮 `2/6`。该单期结果仅作为前向样本，禁止据此调整权重、改写密封记录或宣称算法提升。

1. `2026084` 及以前的数据已被观察，不再作为独立留出证明。
2. 后续新期开奖只做前向验证，预测必须在开奖结果发布前固定。
3. 未达到可用门槛时必须显示“尚未证明优于随机基线”。
4. 不以单次命中、短窗口最高值或“中大奖”个案替代统计证据。
5. 前向可用门槛与滚动回测一致：至少 100 个已结算样本；红球点位提升的95%下界与前后半段提升都大于0；独蓝 Wilson 95%下界及前后半段命中率都高于 `6.25%`；红蓝同时满足才标记整体可用。
