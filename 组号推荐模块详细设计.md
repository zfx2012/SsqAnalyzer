# 组号推荐模块详细设计

> 版本：v2.0（项目定稿）  
> 日期：2026-07-28  
> 适用项目：SsqAnalyzer  
> 技术栈：WPF / .NET 10  
> UI 方案：方案 C「双栏组号工作台」+ 方案 A「期号数据轨道」

## 1. 定稿结论

组号推荐页是按目标期号工作的数据汇总与组合工作台。它读取走势图、复式票统计、点位和杀号四类上游数据，按可配置规则生成双色球组合。

本次确定以下原则：

1. 不要求四项数据全部就绪，0～4 项数据均可执行组号。
2. 四个数据源无论是否有数据都可以点击。
3. 顶部按期号展示数据轨道，清楚标识可用、未生成、已过期和异常状态。
4. 缺少数据只改变参与组号的数据源和提示文案，不禁用组号按钮。
5. 走势图在页面中只显示粗略预览，正式组号读取五类走势图的冷号和预测斜连号码。
6. 复式票统计读取完整红球、蓝球统计数，并按“统计数越小，策略优先级越高”的用户规则提供组号因子。该因子仅是用户策略，不代表客观开奖概率。
7. 点位只读取 6 个红球点位，不读取蓝球。
8. 杀号只读取杀号后剩余的红球和蓝球大底。
9. 具体组号规则在项目末期继续完善；当前设计先固定输入契约、合法性约束、可复现性和解释结构。

## 2. 与参考文档的差异

参考文档按 PySide6 + SQLite 设计，并包含本项目当前不采用的输入和阻断规则。本项目版本作如下调整：

| 参考设计 | 本项目定稿 |
|---|---|
| PySide6 + SQLite | WPF + .NET 10，复用现有服务和资源字典 |
| 点位包含多类派生号码 | 点位输入严格限定为 6 个红球点位 |
| 点位或杀号缺失时禁止组号 | 任意数据缺失都不阻断组号 |
| 已就绪与未输出采用不同可点击规则 | 所有状态始终可点击 |
| 复式票只概括低于平均值号码 | 保存 01～33 红球、01～16 蓝球的完整统计数组，包括统计数为 0 的号码 |
| 组号结果可随机变化 | 相同目标期、相同输入快照和相同规则版本必须得到相同结果 |

## 3. 页面唯一任务

用户进入目标期后，应在一个页面内完成以下闭环：

```text
查看本期数据状态
    ↓
选择数据源并核对证据
    ↓
设置注数、预算和规则版本
    ↓
生成组合
    ↓
查看每组号码使用了哪些数据与规则
```

页面不是四个上游模块的复制品，也不在组号页重新计算或编辑上游业务结果。

## 4. 最终页面结构

### 4.1 顶部标题区

- 页面标题：`组号推荐`
- 辅助说明：`按期号汇总已有证据；数据不完整也可以组号`
- 目标期号：默认取最新开奖期号 + 1，可切换历史或未来目标期。
- 刷新：重新读取当前期的四类输入快照，不自动执行杀号或点位算法。

### 4.2 期号数据轨道

期号轨道是本页的核心识别元素，按固定顺序显示：

```text
走势图 ── 复式票统计 ── 6 个点位 ── 杀号大底
```

每个节点显示数据量或简要状态，例如：

- 走势图：`5 视图`
- 复式票统计：`36 票`
- 点位：`已生成`
- 杀号：`未生成`

交互规则：

- 整个节点始终可点击，并切换到对应上游页面。
- 可用状态使用绿色，未生成使用金色，过期使用橙色，异常使用红色。
- 状态颜色只表示数据状态，不表示号码好坏或预测置信度。
- 轨道右上角汇总：`3 项可用 · 1 项未生成 · 所有节点均可点击`。

### 4.3 三栏工作台

宽屏状态下采用三栏：

| 区域 | 作用 | 建议宽度 |
|---|---|---:|
| 左栏：本期数据源 | 切换当前查看的数据源，展示数据量和状态 | 220～250 px |
| 中栏：数据证据 | 展示选中数据源的粗略内容和快照信息 | 自适应，最小 420 px |
| 右栏：组号与结果 | 参数、生成按钮、结果预览和解释 | 360～410 px |

左侧数据卡单击用于切换中央详情；卡片右侧箭头用于打开对应上游页面。这样既保留工作台连续浏览，又满足“有无数据都可以进入对应页面”的要求。

### 4.4 响应式布局

项目主窗口默认宽度为 1100 px，扣除 200 px 导航后无法稳定容纳三栏。因此必须支持布局切换：

| 内容区宽度 | 布局 |
|---:|---|
| ≥ 1050 px | 左、中、右三栏同屏 |
| 760～1049 px | 左栏 + 中栏；组号与结果移到下一行并占满宽度 |
| < 760 px | 数据轨道允许横向滚动，三个区域纵向排列 |

WPF 可在 `GroupPage.SizeChanged` 中只切换预定义的 Grid 行列位置，不复制业务状态。

## 5. 四类输入数据契约

### 5.1 统一快照头

每项数据必须带有独立快照信息：

| 字段 | 类型 | 说明 |
|---|---|---|
| `TargetIssue` | `int` | 该快照服务的目标期号 |
| `AsOfIssue` | `int` | 计算时使用的最后一期开奖 |
| `GeneratedAt` | `DateTimeOffset` | 生成时间 |
| `SnapshotId` | `string` | 内容哈希或稳定标识，用于结果复现 |
| `RuleVersionId` | `string` | 上游算法版本；没有则使用 `manual` |

不得以页面控件、颜色或渲染后的文本作为组号输入。

### 5.2 走势图输入

页面粗略预览：

- 最近 5 期开奖红球与蓝球；
- 五类视图各自的冷号数量、斜连候选数量；
- 数据截至期和生成时间。

实际组号输入严格来自五类视图：

1. 基本走势图；
2. 历史同期图；
3. 012 路图；
4. 奇偶图；
5. 周期图。

每类视图提供：

```csharp
public sealed record TrendSignalSnapshot(
    TrendViewKind View,
    IReadOnlyList<int> ColdRedNumbers,
    IReadOnlyList<int> ColdBlueNumbers,
    IReadOnlyList<int> DiagonalRedCandidates,
    IReadOnlyList<int> DiagonalBlueCandidates,
    int SampleSize,
    string FilterDescription);
```

组号服务只消费结构化号码集合，不从 `MatrixGrid` 或 `DiagonalChainRenderer` 的视觉对象反向读取数据。当前项目尚未暴露统一的走势图信号模型，需要新增快照构建器。

### 5.3 复式票统计输入

来源为当前 `ITicketRepository`：

- `CurrentType` 必须是 `LotteryType.SSQ`；
- `Period` 对应目标期号；
- `Tickets` 是本次统计的原始复式票集合。

快照必须包含：

```csharp
public sealed record TicketStatsSnapshot(
    int TargetIssue,
    int TicketCount,
    IReadOnlyList<int> RedCounts,   // 固定 33 项，索引 0 对应号码 01
    IReadOnlyList<int> BlueCounts); // 固定 16 项，索引 0 对应号码 01
```

要求：

- 统计数为 0 的号码也必须保留，不能只保存出现过的号码。
- 红球与蓝球分别排序、归一化和评分。
- 初始策略采用升序优先：统计数越小，策略分越高。
- 页面同时显示号码与统计数，例如 `02·0`、`06·1`，不能只显示号码。
- 统计行算法应从 `TicketsPage.BuildStatsRow` 中抽取到可测试的服务，避免组号页复制 UI 计算。

### 5.4 点位输入

组号只读取 `PositionPrediction.RedPoints`：

```csharp
public sealed record PositionGroupSnapshot(
    int TargetIssue,
    IReadOnlyList<int> RedPoints);
```

有效性条件：

- 恰好 6 个号码；
- 号码互不重复；
- 每个号码在 01～33 范围内；
- `PositionPrediction.Issue == TargetIssue`。

以下现有字段不参与本版组号：

- `FormulaBlue`
- `SingleBlue` / `DoubleBlue` / `TripleBlue`
- 点位 ±1 扩展范围

### 5.5 杀号输入

来源为 `KillReport` 的剩余号码池：

```csharp
public sealed record KillPoolSnapshot(
    int TargetIssue,
    IReadOnlyList<int> RemainingRedNumbers,
    IReadOnlyList<int> RemainingBlueNumbers);
```

字段映射：

- `KillReport.RecommendedRedBalls` → `RemainingRedNumbers`
- `KillReport.RecommendedBlueBalls` → `RemainingBlueNumbers`

组号页不重新根据被杀号推导剩余池，也不自动调用 `IKillEngine.Execute()`。只有用户在杀号页明确执行后产生的报告才算“有数据”。

当前杀号引擎 MVP 默认蓝球全集也应原样进入快照，后续蓝球杀号上线时无需修改组号输入契约。

## 6. 数据状态模型

```csharp
public enum GroupSourceState
{
    Ready,
    Missing,
    Stale,
    Invalid
}
```

| 状态 | 判定 | 默认是否参与组号 | 是否可点击 |
|---|---|---:|---:|
| `Ready` | 目标期一致且数据合法 | 是 | 是 |
| `Missing` | 没有该期快照 | 否 | 是 |
| `Stale` | 存在快照，但目标期早于当前期 | 否，用户可手动启用 | 是 |
| `Invalid` | 数量、范围或内容校验失败 | 否 | 是 |

生成按钮不依赖状态组合：

- 4 项可用：`使用 4 项数据开始组号`
- 1～3 项可用：`使用 N 项数据开始组号`
- 0 项可用：`仅使用基础规则开始组号`

0 项数据时，红球候选池为 01～33，蓝球候选池为 01～16，只应用合法性规则和用户输入参数。

## 7. 组号流程

```text
1. 读取目标期四类快照
2. 校验并形成数据状态
3. 冻结本次启用的快照 ID
4. 建立红球 01～33、蓝球 01～16 基础池
5. 若有杀号大底，则用剩余池替换对应基础池
6. 将走势图冷号/斜连、复式票反向统计、6 个点位转换为策略特征
7. 应用基础合法性约束
8. 应用当前组号规则版本
9. 生成、去重并稳定排序
10. 输出号码、来源说明、规则版本和警告
```

### 7.1 当前必须实现的合法性规则

- 每注恰好 6 个红球和 1 个蓝球；
- 红球范围 01～33；
- 蓝球范围 01～16；
- 同注红球不重复；
- 结果之间不重复；
- 注数和预算不能互相矛盾；
- 候选池不足时不生成非法补位号码，直接返回明确错误。

### 7.2 暂不定稿的策略规则

以下内容留到项目末期通过规则版本完善：

- 五类走势图之间的权重；
- 冷号与斜连号码的最低/最高采用数量；
- 复式票统计数到策略分的映射；
- 6 个点位每注采用数量；
- 杀号大底是硬池还是允许冲突回补；
- 奇偶、大小、三区、012 路、和值等结构性偏好。

UI 只能显示实际启用的规则，不提前写死“强制命中”“提高概率”等表述。

## 8. 可复现性与结果解释

当前 `AnalysisService.GetGroupRecommendations` 使用 `Random.Shared` 生成冷热号组合，只能视为旧占位实现，不能作为正式组号引擎继续扩展。

正式结果必须满足：

```text
相同 TargetIssue
+ 相同输入 SnapshotId 集合
+ 相同 RuleVersionId
+ 相同用户参数
= 相同组号结果及顺序
```

每个结果至少显示：

- 6 个红球和 1 个蓝球；
- 使用的数据源；
- 跳过的数据源及原因；
- 规则版本；
- 关键证据摘要；
- 输入快照组合 ID。

结果解释示例：

```text
使用：走势、复式票统计、点位
跳过：杀号大底（本期未生成）
证据：冷号交集 02/06；低统计号 14/23；点位命中 3 个
规则：draft-v1
```

## 9. 项目内服务设计

首期实现采用以下最小边界，避免继续把统计和组号逻辑堆入 `GroupPage.xaml.cs`：

```csharp
GroupService.GetInputs(targetIssue);
GroupService.Generate(targetIssue, ticketCount);
GroupInputStore.SavePosition(prediction);
GroupInputStore.SaveKill(report);
```

职责划分：

| 组件 | 职责 |
|---|---|
| `GroupPage` | 交互、布局和渲染，不计算复式统计或组合 |
| `GroupService` | 聚合四类输入、校验状态并生成可复现结果 |
| `GroupInputStore` | 应用期内按期号保存点位和杀号的明确输出 |
| 规则组件 | 独立计算特征和得分，项目末期完善 |

快照如需跨应用重启保留，应写入项目现有安装目录下的 `data/` 路径并通过 `AppPaths` 统一管理，不迁移到 `%LocalAppData%`。

## 10. 页面导航

当前 `MainWindow.Nav_Click` 同时处理按钮样式和页面切换。为了让数据轨道安全复用导航逻辑，应提取统一方法：

```csharp
public void ShowPage(string tag);
```

导航按钮和组号页的数据节点都调用同一个入口。目标映射：

| 数据节点 | 页面 tag |
|---|---|
| 走势图 | `chart` |
| 复式票统计 | `tickets` |
| 6 个点位 | `position` |
| 杀号大底 | `kill` |

从组号页进入上游页面时应携带当前目标期；若上游页面暂不支持期号定位，则至少保持目标期提示，不静默切换成其他期的数据。

## 11. 视觉规范

最终界面复用 `Themes/DarkTheme.xaml` 中实际定义的浅色主题资源：

| 角色 | 项目资源 | 当前颜色 |
|---|---|---|
| 页面背景 | `BgContent` | `#F7F7F7` |
| 卡片背景 | `BgSurface` | `#FFFFFF` |
| 分隔线 | `BorderDefault` / `BorderSubtle` | `#CCCCCC` / `#DDDDDD` |
| 主操作 | `Accent` | `#0078D4` |
| 红球 | `RedBall` | `#E04848` |
| 蓝球 | `BlueBall` | `#3B82F6` |
| 缺失/提醒 | `Gold` | `#D4943A` |

文字角色：

- 标题与中文正文：`Microsoft YaHei UI` / `Segoe UI`；
- 期号、号码、统计数：`Consolas`；
- 红蓝颜色只用于号码或必要状态，不大面积填充页面。

交互状态：

- 卡片悬停：背景轻微变化并显示手形光标；
- 键盘焦点：2 px `Accent` 描边；
- 选中数据源：浅蓝底 + 蓝色描边；
- 未生成：浅金底，不使用禁用灰；
- 长号码池允许换行，不使用横向截断省略关键信息。

## 12. 首期实现范围

### P0：先把工作台和数据契约做对

> 状态：已于 2026-07-28 完成，并通过 Release 构建、自动验证和真实 WPF 窗口检查。

- 最终页面布局和自适应；
- 目标期与四项状态监控；
- 所有节点可导航；
- 四类输入快照模型；
- 走势图最近 5 期预览；
- 复式票完整红蓝统计；
- 6 个红球点位；
- 杀号剩余红蓝大底；
- 0～4 项数据均可触发基础单式组号；
- 结果包含来源、跳过原因和规则版本。

### P1：项目末期完善组号规则

- 规则配置和版本管理；
- 各类特征权重；
- 复式、胆拖等输出格式；
- 冲突处理；
- 历史回测和规则比较。

## 13. 验收标准

1. 任意一个或多个数据源缺失时，组号按钮仍可用。
2. 四个轨道节点在四种状态下都可以导航。
3. 更换目标期后，不会把其他期的快照标记为可用。
4. 走势图中央预览只显示粗略信息，组号输入包含五类视图的冷号和斜连候选。
5. 复式票快照固定包含 33 个红球统计数和 16 个蓝球统计数，统计数 0 不丢失。
6. 点位快照只含 6 个合法且不重复的红球。
7. 杀号输入使用剩余大底，而不是被杀号列表。
8. 0 项数据时仍能生成满足双色球合法格式的基础组合。
9. 相同输入快照和规则版本重复生成，结果完全一致。
10. 每个结果能说明使用和跳过了哪些数据源。
11. 默认 1100×720 窗口不会出现控件重叠，宽屏下显示完整三栏。
12. 构建和运行验证通过后，才能宣称页面实现完成。

## 14. 风险说明

- 所有历史规律和组号策略都只能作为辅助分析，不构成中奖承诺。
- “统计数越小越优先”是用户定义的组号规则，不等同于客观概率结论。
- 当前已实现 `draft-v1` 基础组号；高级权重、复式/胆拖和跨应用重启的点位/杀号快照持久化仍属于 P1，不应把它们误认为当前已启用规则。
